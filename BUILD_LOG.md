# BUILD_LOG.md — session journal (append-only)

The handoff mechanism between sessions. A fresh Claude Code session reconstructs state from the **latest entry** + `BUILD_PLAN.md`. Newest at top. One entry per session. Never rewrite history — append corrections as new entries.

**Entry template:**

```
## YYYY-MM-DD — <session focus> (milestone Mx)
**Did:** what was attempted.
**Result:** what passed / failed. Verification suite GREEN/RED (+ reason if RED).
**Decided:** any decision (→ ADR-NNNN if consequential).
**Open:** unresolved threads, surprises, things the next session should know.
**Next action:** the single concrete next step.
```

---

## 2026-06-19 — M0 executed end-to-end: data-access proof + L2 + PIT + id-bridge + survivorship (milestone M0 ✅)
**Did:** Ran M0_TASKS T1–T5 in order. **T1 (the [STOP] gate):** fetched `openapi.json` (22 camelCase tools), then proved the Matriks **REST** path live and wired `MatriksAdapter.fetch()`/`smoke_check()`; `make smoke` PASS. **T2:** implemented `L2Store` (DuckDB connect + `bootstrap_schema` from schema.sql + `write_parquet` PK-idempotent round-trip). **T3 (keystone):** implemented `PITAccess.series()` with a non-negotiable `knowledge_date <= as_of` filter and unskipped the **PIT-leak detector**. **T4:** built the `IdBridge` resolver (ticker↔ISIN↔kap_oid↔LEI) that refuses+logs ambiguity. **T5:** implemented `PITAccess.universe()` + the survivorship/W2 mechanism. Also strengthened the no-fabrication invariant (unreachable ⇒ `SourceUnreachable`, never a placeholder). Added ADR-0002; updated VERIFICATION/BUILD_PLAN.
**Result:** **`make verify` GREEN — 96 passed / 4 skipped** (was 80/5); `make smoke` PASS. New green tests: `test_matriks_live` (6), `l2/test_l2_roundtrip` (3), `test_pit_leak::test_no_read_returns_knowledge_date_after_as_of`, `test_idbridge` (4), `test_survivorship` (2), `test_no_fabrication` (3). Remaining 4 skips are correctly future-milestone: accounting_regime no-straddle (M1), no-network signal AST-scan (M3), provenance soft-edges (M1), fixtures-never-reach-L2 (M1).
**Decided (→ ADR-0002):** Ingestion transport = **REST** `POST /mcp-api/v1/tools/{camelCaseTool}/execute`. **Auth = single `X-API-Key: <username>:<key>` header — adding `X-Client-ID` makes the gateway 500 'Authentication failed'** (proven by elimination; the original stub's extra header was the bug). Response is an MCP envelope; payload = `json.loads(content[0].text)`. Smoke gate is two-mode: **value-match** the two immutable OHLCV anchors (real drift teeth), **reachability-only** for the curated/volatile single-call goldens, **skip** the multi-call composites — no fabricated value-match where data legitimately moves. Writes `data/cache/matriks_smoke_report.json`.
**Open:**
- **The reshape finding:** only the two OHLCV goldens are raw single-call captures that value-match live. `declaration_dates`, `universe`, `foreign_flow` were **reshaped/curated at capture** (golden symbolSearch `data` = live's inner `index` object; declaration golden is a derived extract) and news/foreign-flow are volatile → they prove reachability, not value. **M1 follow-up:** re-capture raw single-call goldens for these so they graduate to value anchors.
- **id-bridge leg = `kap_oid`, not `mkkMemberOid`.** The v1 graph stores `kap_oid`; MKK's `mkkMemberOid` (Matriks/MKK id space) is a separate leg to reconcile when Matriks ingestion lands (M1). All four anchors round-trip clean.
- **Survivorship is the mechanism, not a real name yet.** v1 graph has no time-varying listing (static `listing_status` only: EQUITY_TRADED / NON_EQUITY_ISSUER / EXTERNAL_PARENT, no delisted-with-dates). T5 proves the W2 wall via `universe_membership` (new bitemporal L2 table) with controlled test rows. **M1:** ingest a real delisted BIST name end-to-end with sourced dates.
- schema.sql fix: `window` is a DuckDB reserved keyword → quoted in `betas`/`residual_corr`.
- `PITAccess.graph()` still a stub — deferred to M1 when time-varying edges (MEMBER_OF/CONTROLS history) carry `knowledge_date`. id-bridge reads the graph directly (identity is not a time-varying signal read), same posture as L2Store on the quant side.
**Next action:** Begin **M1 — clean return series.** First slice: the Matriks OHLCV + corporate-actions + dividends ingestion adapter → L2 `prices`/`total_returns` (bitemporal), then USD-primary total-return construction with the EREGL/ASELS bedelsiz golden-master reconciliation (BUILD_PLAN M1 exit). While there, re-capture the raw single-call goldens to upgrade them to value anchors.

## 2026-06-19 — v2 build kickoff: plan + protocols laid down (pre-M0)
**Did:** Authored the v2 build scaffolding — `CLAUDE.md` (constitution: invariants, data-access contract, session protocol), `BUILD_PLAN.md` (milestones M0–M8 with go/no-go gates), `VERIFICATION.md` (invariant suite + golden masters + promotion-gate mechanics), this log, and `decisions/ADR-0001` (L1 store = Kuzu). Then pre-snapshotted **10 golden samples** from the Matriks MCP to `tests/golden/matriks/` (OHLCV around the EREGL/ASELS rights ex-dates, KCHOL declaration dates, adjusted-vs-unadjusted FY2024 fundamentals, USD/TRY + XU100 factor series, GARAN foreign flow, news feed, BIST30 universe) + a MANIFEST mapping each to its invariant/gate. Then **scaffolded the v2 repo**: `src/tmkg/pit/` (bitemporal access gateway — the keystone), `src/tmkg/l2/` + `schema.sql` (DuckDB quant store, all tables bitemporal incl. signal_registry), `src/tmkg/ingest/` (adapter contract + Matriks stub + audit-report writer), placeholder packages `returns/`/`factors/`/`signals/`; the verification harness (`Makefile`, `scripts/verify.sh`, `scripts/smoke_data_access.py`, `pytest.ini`, `tests/invariants/` §5 guards + `tests/golden/test_golden_integrity.py`); wired the Matriks connector (`.mcp.json` via `${MATRIKS_API_KEY}`, real key in gitignored `.env`); updated `requirements.txt`; wrote `M0_TASKS.md`. Stubs raise `NotImplementedError` with `TODO(M0)` — no pillar logic yet.
**Result:** Planning docs complete and cross-checked. Matriks MCP verified **reachable from Cowork**; all 10 golden JSON files valid; reconciliation answers pinned — EREGL −1.40% across the 2024-11-27 rights ex-date (no gap → back-adjusted), KCHOL FY2024 revenue adjusted/unadjusted ratio 1.31, declaration dates back to FY2008, AKDE/MKK foreign-flow license live. Verification suite: N/A (not yet wired — that is M0).
**Decided:** L1 store = KuzuDB (ADR-0001). Data access = same MCP connectors as Cowork, via checked-in `.mcp.json`; architecture is MCP → ingestion adapter → local Parquet/Kuzu cache; signal layer never hits the network; market data is never fabricated.
**Open:**
- **Reachability proven from Cowork, NOT yet from Claude Code.** The golden samples prove the *data* exists and the *Matriks* tools work here; they do not prove Claude Code's environment can reach it. Re-fetching to match the golden samples is M0 T1, the hard gate. **(Update: auth now fully documented from `Matriks_MCP_Dokumani.pdf` — REST at `mcp-api/v1/tools/{tool}/execute` with `X-API-Key: <username>:<key>` + `X-Client-ID`, and header-auth MCP at `/mcp`. Neither is the `/claude` OAuth endpoint, which is Claude-Desktop-only. `.mcp.json` corrected from the wrong `/claude` Bearer guess to `/mcp` header-auth. The ingestion adapter uses REST — Python cannot call MCP tools; the header/URL helpers are implemented + verified, only the httpx call remains.)**
- **Only Matriks is snapshotted.** EVDS, KAP-direct, GDELT, and the W3 proxies (FRED VIX, EEM, scraped CDS) have no golden sample yet — capture when wired.
- **Two Matriks param quirks documented in the MANIFEST:** `institutionalFlow` ignored `foreignPeriods` (returned latest month); `newsAndEvents` did not strictly honor `category`. Verify specific-period/historic foreign pulls and filter news client-side.
- v1 equities core (802 Companies, Kuzu) is the L1 starting point; confirm it still loads and `pytest` is green before extending.
- Cowork-sandbox Kuzu lock gotcha is a sandbox artifact and *should* not affect native Claude Code on macOS — verify early; if "Operation not permitted" on `.shadow`/`.wal` appears, build on a local path and copy the `.kuzu` file.
**Scaffold verification (this session, in-sandbox):** all new stubs compile (`py_compile`) and import (stdlib-only deps); `PITAccess(None)` raises `PITViolation` (the first real invariant test is green); `.mcp.json` is valid JSON; secret scan confirms `sk_live_` appears **only** in the gitignored `.env`. The full `pytest` suite (incl. the v1 ~70 + new golden/invariant tests) must be re-run natively by Claude Code — the sandbox lacks the venv/duckdb/kuzu.

**Next action:** Execute `M0_TASKS.md` **T1** — `set -a && source .env && set +a`, fetch `https://mcp.matriks.ai/openapi.json` to confirm the REST tool slugs/params, implement `MatriksAdapter.fetch/smoke_check` over the **REST API** (`X-API-Key: <username>:<key>` + `X-Client-ID`), and get `make smoke` to PASS (live response matches the golden samples). Record the working transport in `decisions/ADR-0002`. If unreachable after checking creds + openapi.json → STOP, do not fabricate.
