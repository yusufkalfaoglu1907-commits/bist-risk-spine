# BUILD_LOG.md — session journal (append-only)

The handoff mechanism between sessions. A fresh Claude Code session reconstructs state from the **latest entry** + `BUILD_PLAN.md`. Newest at top. One entry per session. Never rewrite history — append corrections as new entries.

**Entry template:**

```
## YYYY-MM-DD — <session focus> (milestone Mx)
**Did:** what was attempted.
**Result:** what passed / failed. Verification suite GREEN/RED (+ reason if RED).
**Decided:** any decision (→ ADR-NNNN if consequential).
**Open:** unresolved threads, surprises, things the next session should know.
**Next action:** the single concrete next step.
```

---

## 2026-06-27 (cont. 12) — M9.2 incremental core BUILT (window + freshness)
**Did:** Built the M9.2 incremental-update brain `tmkg/ingest/incremental.py`: `incremental_window` (pure — `None` when current, full backfill when no history, else an overlap re-fetch from the last held bar so late/revised bars are absorbed idempotently) + `classify_freshness`/`freshness_report` (split listed names into current / stale / missing by their latest L2 bar). `scripts/freshness.py` (read-only scan; nonzero exit if stale/missing).
**Result:** verify GREEN 461 passed / 1 skipped (+7 `tests/ingest/test_incremental.py`). Live: **569 current, 1 stale (ISATR — share-class alias, bars under ISCTR), 161 missing** (of 731 — the non-equity + un-onboarded names).
**Decided:** the freshness scan targets the daily runner (refresh only stale names via their incremental window, skip current) — efficient daily top-up, idempotent overlap. The 161 "missing" overlaps the onboarding queue (non-equity + new names); equity-scoping resolves both.
**Open (M9 tail):** the thin daily *runner* (loop targeted per-name pull over the stale set + regime-aware refit trigger) and the equity-scope refinement. The cores for detect/onboard/queue/heartbeat/incremental are all built + tested.
**Next action:** the daily runner wrapper + equity-scoping, or hand off — M9 is substantially complete. Repo GREEN.

---

## 2026-06-27 (cont. 11) — Targeted vendor-lag-aware onboarding executor BUILT
**Did:** Built `onboarding.onboard_symbol` — the correct per-name quant-onboarding executor (fixes the "whole-universe scripts are wrong for one name" finding). Steps for ONE identified symbol: market-data check → prices → total_returns → universe_class; **vendor-lag-aware** (if `market_data_status`→`not_carried_yet`, returns `deferred_vendor_lag` WITHOUT erroring — retry later); refuses cleanly on unresolved sector; defers the factor refit until the name has ~60-session history. Per-symbol functions (`ingest_prices`/`build_total_returns`/`ingest_universe_class`), pulls only the one name.
**Result:** verify GREEN (pending; +1 offline test = vendor-lag short-circuit via fake adapter). **Live `onboard_symbol('FAIRF')` → `deferred_vendor_lag`** (clean — no error, no failed pull, no fabrication). When Matriks carries FAIRF, the same call proceeds.
**Decided:** new-IPO onboarding is now handled gracefully end-to-end — identity now (done for FAIRF), quant deferred until the vendor catches up, surfaced by the 3c queue + M9.1 heartbeat, executed targeted per-name. No half-onboard, no fabrication (§4).
**Open (M9 remaining):** equity-scope the queue (211 noisy); M9.2 incremental "since last knowledge_date" + regime-aware refit; resume FAIRF when carried. M9 is now substantially complete (3a detect · 3b orchestrate · 3c queue+vendor-lag · targeted executor · M9.1 heartbeat); only M9.2 + the equity-scope refinement remain.
**Next action:** M9.2 incremental mode, or equity-scope the queue. Repo GREEN.

---

## 2026-06-27 (cont. 10) — M9.1 keep-current heartbeat BUILT (schedulable read-only gate)
**Did:** Built **M9.1 — the keep-current cycle** `tmkg/ingest/keep_current.py`: composes the 3a new-listing detector + the three M8.3 health monitors (id-bridge/smoke-drift/registry) + the 3c onboarding queue into ONE read-only status pass. `summarize_cycle` (pure, tested) folds them into an `attention` verdict (+ `health_ok` isolating monitor failures from softer pending-onboarding). `scripts/keep_current.py` prints status, writes `data/cache/keep_current_report.json`, **exits nonzero on attention** — the scheduler hook (cron/launchd snippets in the docstring; wire exit→alert). Investigated equity-scoping the queue first: the authoritative equity universe is `_universe_symbols` = ticker+ISIN+sector, but a brand-new name lacks ISIN/sector until onboarded, and KAP `financial_type` is NOT a clean equity/derivative signal (None spans both) ⇒ equity-scoping a *new* name is a genuine design question (vendor availability + ISIN-class at onboard time), documented as a follow-up rather than guessed.
**Result:** verify GREEN (pending; +5 `tests/ingest/test_keep_current.py`). Live heartbeat: monitors all green, 0 new listings (FAIRF now seeded so no longer "new"), queue 211 → **ATTENTION, exit 1**.
**Decided:** the heartbeat is **read-only and safe to schedule** — it reports/gates, never mutates or auto-onboards (a new IPO's market data lags KAP, so blind pulling is wrong). It's the payload a cron/launchd entry runs after the ingest jobs. The 211-name attention is honest but noisy (non-equity instruments dominate) — equity-scoping is the next refinement to make the alert actionable.
**Open (M9 remaining):** (a) equity-scope the onboarding queue (drop warrants/funds); (b) targeted, vendor-lag-aware per-name onboarding executor (use `ingest_prices`/`build_total_returns`/`ingest_universe_class`, skip `not_carried_yet`); (c) M9.2 incremental "since last knowledge_date" pulls + regime-aware refit; (d) resume FAIRF when Matriks carries it. The composition (3a+3c+M8.3) is done.
**Next action:** equity-scope the queue or build the targeted executor (both make the heartbeat actionable), then M9.2. Repo GREEN.

---

## 2026-06-27 (cont. 9) — Matriks bars root cause = vendor data-lag (NOT outage); M9.3c onboarding queue + vendor-lag classifier BUILT
**Did:** Per user, retried the Matriks bars pull for FAIRF + diagnosed the root cause, then continued building.
**ROOT CAUSE (decisive):** the bars `SERVICE_UNAVAILABLE` is **FAIRF-specific, not a service outage** — GARAN/ASELS bars both returned 62 bars fine (service + auth alive), and **`symbolSearch` for FAIRF (and every name variant) returns 0 results: Matriks does not carry the symbol yet.** This is the expected **new-IPO market-data lag**: KAP registers a company at listing (so 3a detects it), but the market-data vendor adds the tradeable symbol's bars feed days/weeks later. Matriks returns a generic `SERVICE_UNAVAILABLE` for an unknown symbol rather than a clean not-found. So FAIRF's price step is a *transient expected pending* (retry later), not a failure.
**Built M9.3c** to operationalize this + the earlier findings: `tmkg/ingest/onboarding_queue.py` — `onboarding_queue` (every *listed* graph name with an incomplete quant row, via bulk graph+L2 reads — the real retry source, since the 3a detector stops flagging a name once it's seeded) + `classify_market_data`/`market_data_status` (symbolSearch → `carried` / `not_carried_yet` (vendor-lag) / `carried_other_code`). `scripts/onboarding_queue.py [--check-market]`.
**Result:** verify GREEN (pending; +7 `tests/ingest/test_onboarding_queue.py`). Live classifier: **FAIRF→`not_carried_yet`**, GARAN→`carried`, ZKBVR(warrant)→`not_carried_yet`. Real queue: **211 incomplete listed names**.
**Decided / findings (durable):** (1) **FAIRF blocker = vendor data-lag** — retry the price pull periodically; it will succeed once Matriks carries FAIRF. (2) **The queue is scoped to all `is_listed` nodes (211) and is dominated by NON-EQUITY instruments (warrants ZKBVR/ZKBVK, fund codes)** that were never in the equity modeling scope (M3 = ~570 equity names). **Equity-scope filtering is a required follow-up** before the queue drives automated onboarding (signal: equity-class ISIN TRA/TRE, or a KAP financial_type filter — needs investigation). FAIRF is the one genuine equity target. (3) Vendor-lag must be classified distinctly so a scheduler skips/retries rather than erroring.
**Open:** equity-scope the onboarding queue; targeted per-name onboarding executor (using `ingest_prices`/`build_total_returns`/`ingest_universe_class`, vendor-lag-aware); then M9.1 scheduler driven by the 3c queue; M9.2 incremental. FAIRF quant onboarding resumes when Matriks carries it.
**Next action:** continue — equity-scope the queue (or build the targeted vendor-lag-aware executor), then M9.1. Repo GREEN.

---

## 2026-06-27 (cont. 8) — FAIRF live-onboarded (identity DONE; quant pending — Matriks bars outage + new-listing constraints)
**Did:** User authorized the live onboarding of FAIRF (the 1 detected new listing). Onboarded **surgically** (per-name, NOT the whole-universe `--execute` path, which would re-pull ~570 names — a flaw to fix). Canonical graph mutated (`data/tmkg.kuzu`, local/gitignored):
- **kap_identity ✅** — `seed_companies_from_kap` (cached members, offline) → FAIRF Company + Security + ISSUES in the graph (kap_oid `8acae2c5…`, is_listed).
- **gleif_identity — PARTIAL** — `GleifAdapter.match_company` matched FAIRF → **LEI `789000L3O7HSL8JTQW11` (score 1.0)**, written; **id-bridge round-trips clean** (ticker↔kap_oid↔lei). **ISIN pending** — GLEIF returns `no-equity-class` (FAIRF's equity ISIN not in GLEIF yet, common for a fresh IPO) → refused, NOT guessed (§4).
- **sector — pending** — FAIRF absent from the committed `data/reference/sectors.json` (dated export predates the IPO) → IN_SECTOR unlinked, not guessed. Needs a taxonomy re-export.
- **universe_prices — BLOCKED** — Matriks `historicalData`/bars returned **`SERVICE_UNAVAILABLE`** ("bars servisi kullanılamıyor") — a transient server-side outage. The adapter **failed loud (`SourceUnreachable`) and wrote zero rows** (§4/§8 working exactly as designed — no fabricated prices). Not retried (§8: surface, don't hammer).
- **factor_refit — pending** — depends on prices + a ~60-day window FAIRF (just-listed) does not yet have.
**Result:** FAIRF is **identity-onboarded** (graph node + LEI + id-bridge round-trip) — real, persisted progress; **quant-onboarding pending** on (a) the transient Matriks bars outage, (b) GLEIF ISIN lag, (c) a stale sector taxonomy, (d) inherent insufficient history. No code changed this round (suite GREEN at `b270f05`); only live data + the onboarding/gleif audit reports.
**Decided / findings (durable):** (1) **The whole-universe `--execute` steps are wrong for single-name onboarding** — onboarding must be *targeted* (use the per-symbol `ingest_prices`/`build_total_returns`/`ingest_universe_class`, not `ingest_universe.py`/`run_m3_gate.py` which re-process everyone). Refactor before automating in M9.1/9.2. (2) **The onboarding source should be "graph names missing complete quant stages," not the 3a "brand-new" list** — once FAIRF was seeded, the detector stopped flagging it (kap_oid now in graph) though it's quant-incomplete. (3) A fresh IPO is inherently a *staged* onboard: identity now, prices accumulate, betas only after ~60 sessions. (4) GLEIF/Matriks both reachable this session (GLEIF matched; Matriks **auth** fine — it was the **bars service** that was down, distinct from the 2025-06 auth outage).
**Open:** Resume FAIRF quant onboarding when Matriks bars is back: targeted price pull → total_returns → (refresh sector taxonomy →) universe_class → betas once ≥60d. Then the M9 refactors above, M9.1 scheduler, M9.2 incremental.
**Next action:** retry FAIRF's targeted price pull when Matriks bars service recovers (or user re-runs), then proceed to M9.1. Repo GREEN; canonical graph now holds FAIRF identity.

---

## 2026-06-27 (cont. 7) — M9.3b onboarding orchestrator BUILT (dry-run; live execution gated)
**Did:** Built **M9.3b — the new-listing onboarding orchestrator** `tmkg/ingest/onboarding.py`. Defines the ordered idempotent pipeline (kap_identity → gleif_identity → sector → universe_prices → factor_refit), where each step is a whole-job run that *picks up* the new entity (once step 1 seeds it into the graph, the universe pull + refit include it) and no-ops everyone else. `onboarding_status` verifies completion against the REAL L1 graph + L2 (Company + lei/isin legs + IN_SECTOR · L2 total_returns/universe_membership · betas); `plan_for_entity` returns only the remaining steps. `run_onboarding` is **dry-run by default** (plans + verifies, no network/mutation); `execute=True` runs the remaining steps via subprocess in order, **fail-loud per entity** (stops the chain on first error — no half-onboarded name). `scripts/onboard_new_listings.py [--execute]` → `data/cache/onboarding_report.json`.
**Result:** verify **GREEN 441 passed / 1 skipped** (+5 `tests/ingest/test_onboarding.py` = recipe order/idempotency, real-script-target existence, plan-skips-done-stages, full-plan, empty-when-complete). **Dry-run on the real graph+L2: FAIRF needs all 5 steps** (absent everywhere — correct). Orchestrator plan + completion-verifier work against live state.
**Decided:** the orchestrator is the *recipe + completion check*; steps delegate to the existing tested scripts (one source of truth per adapter). The live `--execute` path **mutates the canonical graph + L2 and hits the network (KAP/GLEIF/Matriks)** → it is opt-in and **user-gated**; I did NOT run it. Default dry-run is safe + offline.
**Open:** **Live onboarding of FAIRF awaits user go-ahead** (`scripts/onboard_new_listings.py --execute` — or step-by-step). After 3b execution closes, **M9.1 scheduler** (cron/launchd/CI running ingest + the M8.3 monitors + 3a detector as a gate) and **M9.2 incremental mode** (since-last-knowledge_date pulls + regime-aware refit trigger) remain.
**Next action:** get user go-ahead to live-onboard FAIRF (or proceed to M9.1 scheduler). Repo GREEN; 3b machinery committed; no canonical mutation yet.

---

## 2026-06-27 (cont. 6) — README refresh + M9 opened: new-listing/IPO detector (3a) BUILT
**Did:** (1) Refreshed `README.md` to the true v2 arc (commit `5ac00d6`): project-status section (v1 core + v2 substrate; 3-pillar alpha NO-GO; repositioned as research substrate + risk spine), a v2 code map, the "three pillars → all NO-GO" outcome replacing the old roadmap, and a "Keeping the data current" section. (2) Answered the user's auto-update question and **opened M9 — operationalization** (build order chosen by user: 3 new-listing onboarding → 1 scheduler → 2 incremental). (3) Built **M9.3a — the new-listing/IPO detector** `tmkg/ingest/new_listings.py`.
**Result:** verify **GREEN 433 passed / 4 skipped** (+7 `tests/ingest/test_new_listings.py`). **Caught a real false-positive bug before shipping:** the first cut diffed on raw **ticker** → 44 false-"new" / 43 false-"retired" because KAP renames/multi-codes tickers (graph `GARAN` is listed upstream as `GARAN, TGB`, blue-chips like HALKB/YKB flagged as retired). Fixed by keying the diff on the **stable `kap_oid`** (id leg, 100% coverage on ticker names) and surfacing genuine ticker renames separately as id-bridge reconciliations. **Real run on the live cache: exactly 1 genuinely new entity — `FAIRF` (FAİR FİNANSMAN A.Ş.) — 0 retired, 0 ticker-changes.** `scripts/detect_new_listings.py` → `data/cache/new_listings_report.json`.
**Decided:** detection keys on kap_oid, never the ticker (the ticker-drift lesson — a future onboarding/id-bridge invariant). Detector is pure-diff + cached-KAP reader + graph reader; reads local cache + L1 only (no network, §4); detection-only (no graph mutation / price pull — that's 3b). Retirement candidates are KEPT (survivorship), only flagged.
**Open:** M9.3b onboarding orchestrator (seed FAIRF: KAP Company/Security + id-bridge → GLEIF LEI/ISIN → sector → universe-class → price history → factor/residual refit; idempotent, fail-loud) — the next slice; then M9.1 scheduler, M9.2 incremental mode. The live-network onboarding steps reuse existing adapters/scripts per kap_oid subset.
**Next action:** build **M9.3b** — the onboarding orchestrator that actually onboards FAIRF end-to-end through the existing adapters. Repo GREEN; 3a self-contained and committed.

---

## 2026-06-27 (cont. 5) — M8.2 linkage-graph propagation BUILT (M8 core slices COMPLETE) + cache tidy-up
**Did:** (1) Tidy-up commit (`fc4cc69`): swept the working tree of accumulated §3 audit reports — 8 timestamp-refreshed smoke/ingestion reports + the untracked M6-session evidence (gdelt + m6_event_diffexp reports). (2) Built **M8.2 — linkage-graph propagation**, the genuinely *non-cross-sectional* use of the linkage graph: `tmkg/risk/linkage_propagation.py` (pure) = **ownership look-through** (`propagate_ownership_shock` — a held name's idiosyncratic shock flows UP to holders ∝ `HOLDS_STAKE.pct`, compounding over multi-hop chains, cycle-guarded) + **control blast-radius** (`control_blast_radius` — reachability over the verified `CONTROLS` DAG, controllers-up/controlled-down). `tmkg/risk/run_linkage.py` reads the L1 Kuzu edges (graph-agnostic — takes a `con`, so the `risk` no-network invariant stays trivially satisfied; pct→fraction normalised explicitly) + `scripts/run_linkage_shock.py` CLI → `data/cache/m8_linkage_shock_report.json`.
**Result:** verify **GREEN 429 passed / 1 skipped** (+9 `tests/risk/test_linkage_propagation.py` = multi-hop reconciliation, direct+indirect sum, cycle-safe, confidence-prune, out-of-range raise, blast-radius reachability, injected runner). Real run: ARCLK −20% + FROTO −15% → **KCHOL −15.5% look-through** (0.4853·−0.20 + 0.3865·−0.15, exact). The two edge types correctly differ: KCHOL *owns* 38.65% of FROTO (HOLDS_STAKE→propagates) but doesn't *control* it (no CONTROLS edge, <50%) → blast-radius 0.
**Decided:** M8.2 is a **risk** tool — structural look-through exposure (who is mechanically exposed), explicitly **NOT** the linked-firm co-move *prediction* that was NO-GO (ADR-0006). No Sharpe/gate/registry write. Honest caveat surfaced in every report: look-through weights by stake fraction only, not the stake's share of the holder's total assets (no fundamentals yet) ⇒ upper-bound structural exposure. HOLDS_STAKE C→C is sparse (14 edges, mostly holding→sub) but real; CONTROLS is the dense 212-edge verified DAG. **M8 core slices (M8.1 + M8.2 + M8.3) all COMPLETE.** The risk spine = macro channel re-pricing (M8.1) + idiosyncratic ownership-graph propagation (M8.2), with three standing health monitors (M8.3).
**Open:** Only the optional advanced layers remain in M8 (GraphRAG/NL-explanation over L1, OpenSanctions, GNN — only on explicit direction; deferred). Future enhancements noted: PIT-as-of ownership (currently latest-known snapshot, like IdBridge); scale look-through by the stake's share of holder assets once fundamentals/market-cap land.
**Next action:** none queued — M8 core done, repo GREEN, working tree clean. Await user direction (an advanced layer, or a new direction). 

---

## 2026-06-27 (cont. 4) — M8.3 hardening COMPLETE: signal_registry hygiene monitor BUILT
**Did:** Closed M8.3 with the third standing monitor — **`signal_registry` hygiene** (`tmkg/monitor/registry_hygiene.py`). Sweeps the bitemporal verdict ledger (read via `PITAccess` at as_of, §5 — full visible history) for **incoherent** rows: `promoted=True` while the gate components don't clear (beat_baselines false / DSR / PBO missing); `n_trials` null/<1 (the M4 D1 required-haircut invariant); `pbo` outside [0,1] or non-finite DSR. Surfaces (never fails on) multi-version signals + the latest verdict per signal — legitimate bitemporal re-evaluation is versioning, not a dup (the PK already forbids exact dups). `scripts/monitor_registry.py` CLI → `data/cache/registry_hygiene_report.json` (§4).
**Result:** verify **GREEN 420 passed / 1 skipped** (+8 `tests/invariants/test_registry_hygiene.py` = 5 teeth tests proving each check bites on a synthetic bad row + multi-version-surfaced + real-ledger-clean; skips real-L2 leg if the gitignored db is absent). Real run: 2 rows (m5/m6, both NO-GO), 0 promoted, PASS.
**Decided:** **M8.3 substrate-hardening COMPLETE** — three monitors now make the substrate's standing health observable + regression-guarded, all pure no-network local reads under `tmkg/monitor/` (in the no-network L3 AST invariant): id-bridge health (the §5 SPOF), data-source drift (recorded smoke outcomes), registry hygiene (verdict-ledger coherence). Each = monitor module + §4 report + CLI + regression invariant. BUILD_PLAN M8.3 marked ✅.
**Open:** M8.2 linkage-graph propagation (CONTROLS DAG blast-radius via `schema/integrity.py` + `PITAccess.graph` Cypher over L1 Kuzu — the genuinely non-cross-sectional use of the linkage graph) is the remaining M8 slice. Pre-existing dirty cache reports (prior-session gdelt/m6 audit JSONs + drifted smoke reports) still untracked — offered a tidy-up commit, not yet done.
**Next action:** build **M8.2** (the last open M8 slice), or per user. Repo GREEN; M8.3 committed.

---

## 2026-06-27 (cont. 3) — M8.3 hardening: data-source drift monitor BUILT
**Did:** Continued M8.3. Built the **data-source drift monitor** `tmkg/monitor/smoke_drift.py` — aggregates the per-adapter `<source>_smoke_report.json` outcomes (matriks/evds/fred/worldgovbonds/gdelt) into one health surface. Key design call (§4): the adapters' `smoke_check()` guards hit the **network**, so the aggregator does NOT re-run them — it reads the **recorded** outcomes from the local cache (the `drift` list each smoke run writes), keeping the monitor a pure no-network local read that fits `tmkg/monitor/`. Per-source status = drift / missing / stale (>max_age_days, warn-only) / ok. `scripts/monitor_smoke_drift.py` CLI → `data/cache/smoke_drift_report.json` (§4).
**Result:** verify **GREEN 411 passed / 4 skipped** (+2 `tests/invariants/test_smoke_drift.py` = no-recorded-drift + core-reports-present; the extra skips vs last run are the live network-drift tests auto-skipping after the heavy pull, by design). Real run: all 5 sources **ok**, 0 drift / 0 missing / 0 stale → PASS.
**Decided:** drift (a contract change a live run already caught) and a missing core report are hard fails; staleness is a surfaced warning only (smoke cadence is environmental, would flake CI). The live network smoke stays in `make smoke` / the live-drift tests; this monitor is the standing *surface* over their recorded results, so a caught drift can't sit unnoticed in the cache.
**Open:** M8.3 last slice = **`signal_registry` hygiene** (dup/stale verdict-row check over L2). M8.2 linkage-graph propagation still queued.
**Next action:** build the `signal_registry` hygiene monitor to close M8.3, then M8.2 (or per user). Repo GREEN; slice self-contained and committed.

---

## 2026-06-27 (cont. 2) — M8.3 hardening: id-bridge health monitor BUILT
**Did:** User directed: commit M8.1, then prioritize M8.3 hardening. Committed the ADR-0006 + M8.1 work (`a363306`). Then built the **id-bridge health monitor** — the §5 single-point-of-failure made observable + regression-guarded. New package `tmkg/monitor/` + `tmkg/monitor/idbridge_health.py`: sweeps the **whole 730-name ticker universe** (not just the 4 anchors `test_idbridge.py` checks) → per-leg coverage + **collision detection** (any id value shared by >1 company = ambiguous identity, the exact failure the resolver refuses on, surfaced proactively) + a full **round-trip sweep** (defense in depth). `scripts/monitor_idbridge.py` CLI → `data/cache/idbridge_health_report.json` (§4). Pure L1 read (no network/L2/mutation); `monitor` added to the no-network L3 AST invariant.
**Result:** verify **GREEN 412 passed / 1 skipped** (+3 `tests/invariants/test_idbridge_health.py` = passes-on-real-graph + zero-collision + coverage-floors; skip cleanly without the v1 graph). Real run: **730 ticker names, coverage isin 0.826 / kap_oid 1.000 / lei 0.919, 0 collisions, 730 round-trips ok / 0 broken / 0 ambiguous → PASS.** Full 730-name sweep runs <2s.
**Decided:** regression floors set **below current real coverage** (isin 0.80 / kap_oid 0.99 / lei 0.88) as decay guards, not aspirational targets — documented that the README's "ISIN 100%" is on the stricter *equity-traded subset*, while this monitor guards all ticker names. With 0 collisions the round-trip sweep is guaranteed to pass (exact-value resolve can't land on another company), so coverage + collisions are the live signal; the sweep is kept as a forward guard against a future schema change.
**Open:** M8.3 remaining slices — a **data-drift smoke aggregator** (run every adapter `smoke_check()` and report drift in one place) and **`signal_registry` hygiene** (dup/stale-entry check). M8.2 linkage-graph propagation still queued.
**Next action:** continue M8.3 (drift-smoke aggregator next, then registry hygiene) or pick up M8.2 — user's call. Repo GREEN; slice self-contained and committable.

---

## 2026-06-27 (cont.) — M8 opened (non-alpha): M8.1 scenario re-pricing tool BUILT
**Did:** User directed pursuit of the non-alpha track — "M8 hardening / a non-cross-sectional use of the linkage graph (risk/scenario tooling)." Scoped M8 in `BUILD_PLAN.md` (M8.1 scenario re-pricing · M8.2 linkage-graph propagation · M8.3 hardening) and built **M8.1**: lifted the §240 channel-stress engine out of the (concluded) M6 event runner into a first-class risk tool under new package `tmkg/risk/` — `scenarios.py` (`Scenario` = signed channel-shock vector + a tier-tagged stylized library on the unit-homogeneous fractional channels {market,fx,rates_cds,energy,holding} + `scenario_from_factor_returns` empirical path), `repricing.py` (`latest_exposure_tensor` from L2 betas · `realized_channel_shock` from real factor *levels* in native units, method-aware: simple/log→%, diff→level-change, level→mean-flow · reuses the audited `channel_stress_pnl`), `run_scenarios.py` (PIT runner → §4 JSON report). `scripts/run_scenarios.py` CLI.
**Result:** verify **GREEN 409 passed / 1 skipped** (+11 new `tests/risk/test_scenario_repricing.py` = reconciliation-to-the-penny + §4 honesty: coverage surfaced, NaN exposure→0 impact, unmodelled channels never zero-filled, all-unmodelled raises, library well-formed, empirical-shock units). Added `risk` to the no-network L3 AST invariant. **Real run** (`scripts/run_scenarios.py 2026-06-15 2025-03-18 2025-03-25`): re-priced **573 names** across all 6 channels against the stylized library AND an **empirical re-pricing of the real Mar-2025 İmamoğlu-shock window** (shock = market −10.1% · CDS +67.8 · **foreign_flow −443.6 USD-mn** [the real outflow signature, matches the M2 −444] · holding −8.5%). Worst-hit names shift sensibly between the stylized FX shock (high-USDTRY-beta) and the real multi-channel episode (KLRHO/CRFSA). **No L2 write, no `signal_registry` row, no Sharpe — it's a risk tool (re-pricing, not prediction); the exit gate is the hand-checked reconciliation, which the tests pin.**
**Decided:** the §240 risk-spine is now a standing, usable product, not a by-product bolted to dead alpha. Stylized scenarios deliberately exclude foreign_flow/sector to avoid a units-mismatch fabrication (§4); the **empirical path is the trustworthy episode re-pricing** (real native-unit factor moves). Risk tools never write a tradeable claim to the registry.
**Open:** M8.2 (linkage-graph propagation through the CONTROLS DAG / HOLDS_STAKE — the genuinely non-cross-sectional use; reachable PIT-honestly via `PITAccess.graph` Cypher over L1 Kuzu) not yet built. M8.3 hardening (id-bridge monitor, drift checks, registry hygiene) standing.
**Next action:** build **M8.2** — CONTROLS group blast-radius propagation layered onto the scenario tool (a parent/sector shock cascades to controlled names), reusing `schema/integrity.py` adjacency. Or pause for user direction on M8.2 vs M8.3 priority. Repo GREEN; this session's slice is self-contained and committable.

---

## 2026-06-27 — M7 NO-GO RATIFIED (ADR-0006); cross-sectional alpha search CONCLUDED; deliverable repositioned as research substrate + risk spine (project-level [STOP] closed)
**Did:** User ratified the **M7 supply-chain/linkage NO-GO** (all three tiers) surfaced 2026-06-26, and with it the **project-level [STOP]**: all three alpha pillars are now NO-GO. Verified the green baseline first (`make verify` → **395 passed / 4 skipped, GREEN**). Wrote **ADR-0006** (full evidence: tier-1 firm-level KAP new-business dead on data feasibility — ~1 listed-independent counterparty/100 disclosures ⇒ ~10–15 tradeable edges/yr; tier-2 intra-group = the already-priced null; tier-3 sector-IO dead OOS at every horizon — in-sample IC +0.24 → OOS −0.005 daily, monthly sample-starved at 13 blocks, OECD-ICIO can't rescue weights over a null). Marked `BUILD_PLAN.md` M7 ⛔ all-tiers NO-GO and M8+ as the only remaining (non-alpha) track with the alpha program CLOSED.
**Result:** Repo GREEN (`make verify` 395/4, `make smoke` was PASS at last run). **ADR-0006 written.** No L2 write (M7 never reached the M4 gate — tier-1 died on feasibility, tier-3 on a pre-gate OOS probe, so there is no `signal_registry` row to land, unlike M5/M6); no schema/adapter change; no fabricated data; **no invariant weakened to force a pass (§8).** The two probe scripts (`scripts/probe_m7_{newbusiness,sector_io}.py`) + two coverage reports (`data/cache/m7_{newbusiness_coverage,sector_io_feasibility}_report.json`) are the durable reproducible evidence.
**Decided (→ ADR-0006):** (1) M7 NO-GO across all tiers. (2) The **cross-sectional firm-linkage alpha search is CONCLUDED** — M5 corr (ADR-0004, edge too thin for costs), M6 event (ADR-0005, structurally dead), M7 supply-chain (ADR-0006). Tradeable cross-sectional alpha on BIST residuals appears exhausted at n≈500 over the available window. (3) **Do NOT** build OECD-ICIO ingestion — it re-weights lead-lags that carry no OOS signal. (4) **Reposition the deliverable** as an honest **research substrate + risk spine**, not a live alpha book. The valuable, real, trustworthy outputs: PIT/bitemporal L1 + PIT wrapper; USD-TR corp-action-adjusted returns + limit-lock/regime/short-eligible state machines (M1); M2 factor/residual machine + M3 substrate guards; the **M4 promotion judge** (the component that earned its keep by cheaply rejecting M5+M6); the **M6 §240 channel-stress risk-spine** (a delivered, working risk product); the GDELT event substrate; tier-1 new-business as an *exposure annotation* (never a tradeable edge). A rigorously-established three-pillar NO-GO **is** the result — worth more than an overfit book that loses money live. The architecture did its job: three plausible theses tested and rejected for the cost of probe scripts, no invariant weakened.
**Open:** The three-pillar alpha program is closed; **no alpha milestone is queued.** Avenues explicitly NOT pursued (would need user direction): ≥2-yr (ideally ≥10-yr for the monthly horizon) window to re-power tier-3/event tests; discrete rare dated-shock sources (KAP/CBRT/economic calendar) for a firm-level event study; the linkage graph used **non-cross-sectionally** (descriptive network / GraphRAG / explanation over L1 = M8 scope). The durable honest-evaluation protocol (non-overlap unit · HAC+block-bootstrap · full search space in DSR n_trials · ≥2-yr window · residual returns · real control leg) is now battle-tested across all three pillars — reusable IP.
**Next action:** none auto-advanced. Only remaining track is **M8 non-alpha hardening / substrate use** (id-bridge monitoring, drift smoke checks, registry hygiene, optional GraphRAG/NL-explanation over L1), opened **only on explicit user direction**. Commit this session (ADR-0006 + BUILD_PLAN + BUILD_LOG + the two probe scripts/reports). Repo left GREEN.

---

## 2026-06-25 (cont.) — Substrate durability diagnostic + two new §5 guards; BOTH flagged "data bugs" reversed under investigation (cross-milestone, no milestone advance)
**Did:** User asked for a durability diagnostic on the two issues flagged at M6 close — "(1) M2 residuals stop at 2025-03-18 (miss the 03-19 shock); (2) FFLOW betas degenerate." Investigated both against the real L2 (`data/l2.duckdb`), wrote two new substrate invariants (`tests/invariants/test_residual_coverage.py`), drove them red→green, and **corrected the record on both claims** (honest reversal, like the M6 oracle-trap finding).
- **FFLOW "degenerate" — REVERSED (not a bug).** Raw mean|β|≈1.9e-5 is **3–4 orders below every other factor**, which *looked* dead — but it is a **units artifact**. The scale-invariant measure (variance contribution `mean|β|·σ_factor`) puts FFLOW at **4.7e-3 = 0.93× the panel median — dead mid-pack.** Its tiny β is exactly offset by σ≈249 (raw weekly net-flow LEVEL, ±856 USD-mn). Foreign-flow **is** being stripped (orthogonality holds by construction). The new non-degeneracy guard therefore uses *contribution*, not raw β (a raw-β floor false-flagged TRCDS5Y too). Genuine residual concerns are softer/M7-scope: FFLOW is **weekly** (52/250 days, ffilled to daily → can't isolate daily flow shocks) and **un-normalized** (raw USD-mn, not scaled to ADV/mkt-cap → cross-regime comparability). Refinements, not a §5 violation.
- **Residual "hole at the shock" — REVERSED (it's the §5 no-straddle rule).** Residuals do NOT stop at 03-18 (that was a **stale frontier**; a later refit extends to 2026-06-15). The 2025-03-19→05-20 gap is **exactly the 39-session `imamoglu_shock_2025` regime warm-up** — `rolling_residuals` confines each window to the end-date's regime and emits nothing until ≥`min_obs`(40) in-regime obs exist (neutralize.py:172). Classified all 201 missing sessions in-span: **118 regime blackouts (§5) · 65 incomplete-factor-panel days (§4, e.g. NATGAS/TRCDS5Y didn't print on a BIST day) · 18 warm-up-tail (panel-gap days inside the early-regime window drop usable obs <min_obs) · 0 genuine corruption.** Backfilling would *weaken* §5 — a §8 hard-stop. So the **test** was corrected, not the data.
**Result:** **verify GREEN — 395 passed / 4 skipped** (+2 new). New guards: `test_no_unexplained_residual_hole` (re-derives the emittability predicate — panel-complete + ≥min_obs panel-complete same-regime sessions in the trailing window — and asserts every emittable session is covered; **teeth-verified**: punching a 20-day mid-2024 emittable block makes it fire with exactly 20, while §5 blackouts / §4 gaps stay green; also catches a stale frontier) and `test_stripped_factors_are_not_degenerate` (scale-invariant contribution floor at 0.05×median; all 18 factors pass). Both skip if the real L2 store is absent.
**Decided:** Neither flagged item is a defect. **No data backfill, no invariant weakened.** The real output is the two missing *classes* of guard the suite lacked (continuity-vs-corruption; factor non-degeneracy) — it previously checked PIT + orthogonality but never coverage or that every stripped factor bites. No ADR (implementation of §5/§4 invariants).
**Open:** (1) **Residual blindness window**: ~40 sessions of NO neutralized residual after *every* regime break — incl. the 03-19 shock itself. By design, but a real signal limitation any post-break event/linkage study must account for (M7 should not assume residual coverage in a fresh regime's first ~2 months). (2) FFLOW weekly cadence + raw units — revisit normalization (÷ADV or mkt-cap) if foreign_flow is leaned on as more than a strip rung in M7. (3) Residual frontier 06-15 vs returns 06-23 (~5 sessions stale) — a refit at the latest as_of would close it; not corruption.
**Next action:** unchanged — open **M7** (supply-chain/linkage). The substrate is durable; both "bugs" were the invariants working. Carry the residual-blindness-window caveat into any M7 event/linkage design.

## 2026-06-25 (cont.) — M6 NO-GO RATIFIED (ADR-0005) → proceed to M7 (milestone M6 closed: alpha NO-GO, stress-spine delivered)
**Did:** User ratified the M6 channel-beta event-spread **NO-GO** (after the 4-agent statistical investigation proved it structural and not rescuable). Recorded **ADR-0005** (full evidence: research net Sharpe −0.105; confounding corr −0.51→+0.02 on residuals but no residual edge; FDR kills all cells; oracle sign a hindsight trap; LLM per-event sign NOT worth building, firm/sector targeting → M7). Marked `BUILD_PLAN.md` M6 ⛔ alpha NO-GO / ✅ risk-spine delivered. Verdict already in `signal_registry`; cost model + inference standard NOT weakened (§8).
**Result:** M6 closed. **Delivered:** GDELT raw-feed ingestion stack (`ingest/gdelt.py` + `ingest_gdelt_events` + resumable `scripts/ingest_gdelt.py` + smoke golden), Q1-2025 `events`/`event_targets` in L2 (71,665/200,716), the differential-exposure runner + gate (`events/run_event_signal.py`, `scripts/run_m6_gate.py`), the **working §240 channel-stress risk-spine**, and a durable honest-evaluation protocol. Two of three pillars (correlation M5, event M6) now NO-GO on real data — the judge architecture is doing its job.
**Decided:** proceed to **M7 — supply-chain / linkage pillar** (the last, scarcest-data pillar). The agents' one endorsed LLM direction — **firm/sector-level event targeting (named entities + supply-chain exposure)** — belongs here, not as a rescue of the dead channel-beta spread. Carry forward: the GDELT event substrate (reusable for firm-level event studies), the residual_corr snapshot (M5), and the honest-evaluation protocol (non-overlap unit · HAC+block-bootstrap · full search space in DSR n_trials · ≥2-yr window · residual returns · real control leg).
**Open:** (1) M2 residuals end 2025-03-18 (miss the 03-19 shock) — extend if event substrate is reused. (2) FFLOW betas degenerate (zero dispersion) — investigate before foreign_flow is used anywhere. (3) Q1-2025 GDELT backfill is pilot-only; a ≥2-yr window would be needed if any GDELT event signal is revisited. (4) LLM firm/sector extraction unbuilt (M7 scope).
**Next action:** open **M7** — read `BUILD_PLAN.md` M7 (supply-chain/linkage: lead-lag through M4 venue-feasible · intra-group-already-priced null · no tier-3 edge in a tier-1 path) and `system-design-v2.md` §s on the supply-chain pillar; scope the firm/sector-level event-targeting + linkage data sources before building.

## 2026-06-25 (cont.) — M6 NO-GO: 4-agent statistical investigation → channel-beta event-spread is NOT rescuable (user-directed; awaiting decision)
**Did:** Per user, launched 4 parallel read-only research agents to empirically test each fix-plan claim against real L2 (Q1-2025 events + betas/residuals/returns). All four converge: the M6 NO-GO is **correct and structural**, not a tuning problem.
- **Agent A (factor confounding / residual control):** the raw-return market spread is a disguised market bet — corr(spread, market move) **−0.51 raw → +0.02 on residuals**. Residualizing is the correct base **but does NOT rescue**: no channel's residual spread bootstrap-CI excludes 0. New defects: **FFLOW betas have ZERO cross-sectional dispersion** (degenerate sort); **residuals end 2025-03-18, missing the 2025-03-19 İmamoğlu crash** (the actual shock) — M2 residuals must be extended.
- **Agent B (salience):** GKG is a total daily floor — **796 events/day, 10.6/11 types every day, max-severity 1.0 every day** ⇒ |tone|-severity is useless as salience. No threshold isolates shocks (degenerate or n→single digits with flipping signs). **Control-leg gone:** fx median control fraction **0.17** (~83% of names highly fx-exposed) — the diff-in-diff has no clean comparison leg. Fix is upstream: discrete deduplicated events (GDELT Events clustering, or **KAP/CBRT/economic-calendar dated announcements**) + require the shocked factor to actually have moved.
- **Agent D (sign attribution + LLM ceiling):** dominant-event attribution un-cancels fx (0→−13) and energy (+1→+28) — mechanism confirmed — **but the oracle is a hindsight trap (50–98% of its edge is day-by-day sign-flipping no model can do; event content predicts realized direction at 0.33–0.50 = coin flip).** Realistic ceiling (best FIXED sign) net of ~10bps cost: **negative on rates_cds, breakeven on fx (+2.6)/foreign_flow (+6.1)**; only market/energy positive — and those are exactly the raw-confounded channels A shows vanish on residuals. **LLM verdict: per-event sign extraction NOT worth building; dedup marginal; salience low; the ONLY worthwhile LLM direction is firm/sector-level targeting = a DIFFERENT (M7-adjacent firm-level) signal.**
- **Agent C (inference rigor / power):** the diagnosis's t=−5 were **overlap + multiple-testing artifacts** — 76 overlapping h=5 windows = only **11 independent**; HAC halves the t-stats, non-overlap/block-bootstrap erase them; of 108 cells, naive flags 53 (≈5 expected by chance) but **0 survive Bonferroni or BH-FDR**. **Power: a 3-month pilot (n_eff≈52) needs annualized Sharpe ≈4.5 to clear the DSR gate — structurally impossible;** a credible test needs **2+ years (n_eff 250–500) and only detects daily SR ≳0.12–0.15**.
**CONVERGENT VERDICT:** the channel-beta-on-GKG-daily-incidence event signal is a clean NO-GO and is **not rescuable** by residual returns (A), salience filtering (B), or LLM sign extraction (D); and a short window can't even judge it (C). The §240 channel-stress **risk-spine works and is a real M6 deliverable**. The promising redirection (B+D) is a **reframed pillar**: discrete/clustered shock events + residual returns + firm/sector LLM targeting + the honest evaluation protocol below.
**Durable statistical-evaluation protocol (reusable for any event signal / M7):** (1) observation unit = **non-overlapping** windows or a single daily P&L series — never count overlapping h-day spreads as independent; (2) **HAC/Newey–West (lag≥window) + moving-block bootstrap (block≥h)** as the headline inference; (3) carry the **full search space** (channels×types×horizons×variants) into the DSR n_trials; (4) **≥2-year** event window; (5) measure drift on **residual** returns; (6) require a real **control leg** + non-degenerate exposure dispersion + factor-move confirmation.
**Next action (USER DECISION, §8):** ratify the channel-beta NO-GO (ADR-0005) + keep the stress spine + proceed to M7 (where firm/sector LLM targeting lives) — vs reframe the event pillar now as a discrete-event firm-level study. Surfaced; nothing auto-advanced.

## 2026-06-25 (cont.) — M6 NO-GO DEEP DIAGNOSIS: the construction collapses into a market-beta bet (no ratify yet, user-directed)
**Did:** Per user ("evaluate in depth"), instrumented the raw differential-exposure spread (read-only on L2; scratchpad `m6_diagnose*.py`) — per channel × horizon and per event-type, free of walk-forward/costs/superposition. **Root-caused the NO-GO; it is NOT "events carry no signal":**
1. **GDELT GKG is per-DOCUMENT and dense** (~770 docs/day; every Q1 day fires many event types at once). The runner superposes all events' prior shocks daily.
2. **Channels with conflicting prior signs across co-occurring types CANCEL EXACTLY.** Proven on **fx** (the most important Turkey channel): fx`+1` (terror/fx-shock/…) spread = **−12.57 bps**, fx`−1` (cbrt) = **+12.57 bps**, net over all fx specs = **0.0** at every horizon. The informative channel self-annihilates because every date carries both signs.
3. **Channels with consistent signs survive** — `market` (−1 in ~every type) and `foreign_flow` (−1). So the *net* signal is dominated by them.
4. The surviving net is a **disguised market-direction / beta bet**, not event alpha: market-channel spread vs contemporaneous market return **corr = −0.51**; Q1-2025 USD window drift **−12.9%**. The "signal" just rides market beta in a down quarter → research net Sharpe −0.105, loses to the static `differential_exposure` baseline (+0.186).
5. **Per-event-type spreads are nearly all significantly NEGATIVE** (natural_disaster −36bps t−3.4, pandemic −32 t−4.4, terror −22 t−4.1, …; only cbrt +6.4 t+1.2) — but this is the SAME market-direction artifact propagating through every type (all share market−1), not 11 independent findings.
**Secondary issues found:** (a) **betas go stale after 2025-03-18** (last beta bar < as_of) → tail events get NaN exposure and `_exposure_matrix` `.iloc[-1]` is all-NaN (channel-stress exposure degraded) → exposure panels should be **ffilled** onto the daily grid (betas are slow rolling estimates; ffill is PIT-legitimate). (b) **market-channel differential exposure is inherently beta-timing, not channel-alpha** — sorting names by XU100 beta and trading the spread = a market call.
**Conclusion:** the §236 cross-sectional design assumes **distinguishable shock events**; GDELT's daily multi-type density violates that ("every day is an event day for every channel"), so the prior-seeded superposition (i) cancels the informative channels and (ii) collapses to market beta. The NO-GO verdict stands, but the pillar is **NOT fairly tested** — the construction needs a **salience/dominant-event filter** (severity / NumMentions threshold to isolate genuine shock days) and per-event signed attribution, plus market-channel exclusion + beta ffill, BEFORE the hypothesis can be judged.
**Next action (user-directed):** decide the redesign — recommended: (1) ffill betas onto the daily grid; (2) drop the `market` channel from the spread (keep it for stress only); (3) add a **salience filter** (per-day dominant event by severity/mention-volume) so the cross-section reflects a real shock, not the daily news floor; (4) resolve channel sign per-event (LLM `gdelt_llm` or severity-weighted netting); then re-run the gate. Verdict + diagnosis recorded; nothing auto-advanced (§8).

## 2026-06-25 (cont.) — M6 EXIT GATE RUN on real Q1-2025 data → NO-GO (surfaced, §8 — awaiting ratification)
**Did:** Completed the pilot Turkey GKG backfill (Q1-2025, full cadence) — **71,665 events / 200,716 targets over all 90 days**, all 11 types (terror/disaster/conflict dominant); 6 transient-failure gaps filled by a `--skip-existing` re-run. Deduped per-doc events to distinct **(event_date, channel, shock_sign)** cross-sections in `event_specs_from_l2` (GKG is per-document, ~770/day; identical-triple specs collapse to the same spread — dedup is shape-preserving and stops raw doc-VOLUME distorting channel weighting; 200k→718 specs, tractable). Ran `scripts/run_m6_gate.py 2025-03-31 2025-01-01`.
**Result — NO-GO (gate run, GREEN 393/4):** the cross-sectional differential-exposure spread does **not** clear M4. Net Sharpe **research −0.105 / venue −0.18 / stress −0.13** — **negative even FRICTIONLESS** (unlike M5, which had a thin frictionless edge that died on costs; this has *no* edge). DSR 0.053 (fails 0.95), candidate **loses to the in-harness `differential_exposure` baseline (+0.186)** and `own_factor_event` (+0.150) — i.e. conditioning the static exposure tilt on GDELT event-days + walk-forward variant churn *hurts*; train Sharpes negative across all 5 folds (no stable structure). **NOT a thin-cross-section escape (§238):** median control fraction **0.888**, **52 OOS dates**, 574 names — a genuine, judgeable test. Verdict landed in `signal_registry` (`m6_event_diffexp`, promoted=False) next to the M5 NO-GO. **The §240 channel-stress SECOND OUTPUT works** (11 scenarios, sensible worst-exposed e.g. armed_conflict→A1CAP/NETAS/NATEN) — the risk-spine deliverable is functional regardless of the alpha verdict.
**Caveats (honest scope of the NO-GO):** used the **coarse inferred-tier taxonomy prior** for channel mapping (sign-only, identical channels per event type — LLM per-event `gdelt_llm` magnitudes NOT built); per-doc events (no cross-doc clustering); Q1-2025 pilot only (but contains the 19-Mar shock + a non-thin cross-section, so not a thin-data artifact).
**Decided:** NOTHING auto-advanced (§8 project gate). Cost model NOT weakened. Surfaced to user for the go/no-go decision (ratify NO-GO→M7 · build LLM extraction as the fair test of the actual hypothesis · extend window).
**Open / Next action:** AWAITING USER RATIFICATION. The interesting wrinkle: the static differential-exposure *baseline* is positive (+0.186) while the *event-conditioned* candidate is negative — the per-event LLM extraction (real signed magnitudes, not the uniform prior) is the genuine test of "under-reaction drift after events" before declaring the event pillar dead. If ratified NO-GO, write ADR-0005 + mark M6 (alpha NO-GO, stress-spine delivered) and proceed to M7.


## 2026-06-25 (cont.) — M6: pilot Turkey GKG backfill RUNNING + gate driver ready (milestone M6 🟢 in progress)
**Did:** User chose the **pilot backfill scope** (Q1-2025, the 19-Mar shock window, full 15-min cadence — cheap first look before a big crawl). Built `scripts/ingest_gdelt.py` (resumable day-granular driver, §8 burst-abort) and `scripts/run_m6_gate.py` (the gate driver: PIT-reads landed events + betas/total_returns → `run_m6_event_signal` → prints verdict + §238 control-survival + §240 channel-stress → lands `signal_registry`). Added a `panel_start` knob to the runner (an event signal is zero off event days, so the gate evaluates over the event-active window, not years of pre-event history).
- **Validated the driver live:** 2025-03-19 landed **1008 events / 2889 targets** into the project L2 (~4.3 min/day). The real L2 already has `betas` (2023-06→2026, all channel→factor factors present: XU100/USDTRY/TRCDS5Y/BRENT/FFLOW/XHOLD) + `total_returns`, so the gate inputs are ready; `events`/`event_targets` tables were absent (store predates M6) — `bootstrap_schema()` adds them idempotently.
- **Q1 backfill is RUNNING** (managed background `brmktx9ti`; day 1 = 830 events). ETA ~6h (89 days × ~4 min).
**Result:** events tests GREEN 28; no production-code regressions. Backfill in flight.
**Decided / operational finding (don't re-trip):** `run_in_background:true` Bash runs in a **sandbox with NO network** (every GDELT GET failed DNS); a network-touching background job MUST also pass **`dangerouslyDisableSandbox:true`** (normal foreground Bash calls keep network, but a 6h crawl must be backgrounded). The first managed launch failed this way and was relaunched unsandboxed.
**Open:** the **M6 exit gate has NOT been RUN** — waiting on the Q1 backfill to finish (DuckDB is single-writer, so the gate must run *after* the crawl, not concurrently). The pilot is a 3-month cluster → the gate may be **thin on OOS dates** (an honest §238 outcome that would itself motivate the fuller backfill). LLM `gdelt_llm` per-event magnitudes still unbuilt (runner uses the inferred prior seed).
**Next action:** when `brmktx9ti` completes, run `PYTHONPATH=src python scripts/run_m6_gate.py 2025-03-31 2024-10-01` and **surface the M6 exit-gate verdict** (§8 — do not auto-advance). If interrupted, resume the backfill with `scripts/ingest_gdelt.py 2025-01-01 2025-03-31 --skip-existing`.

---

## 2026-06-25 (cont.) — M6: real-data event-signal runner built + exit-gate self-test GREEN (milestone M6 🟢 in progress)
**Did:** Built `tmkg/events/run_event_signal.py` — the real-data M6 runner (the L2-touching orchestrator; all stats live in the pure `differential_exposure`/`channel_stress`/M4-judge modules). It: PIT-reads `events`+`event_targets`+`betas`+`total_returns`; assembles per-(event,channel) `EventSpec`s (`event_specs_from_l2`) and per-channel **exposure panels** from L2 `betas` via a channel→factor map (`build_betas_by_channel`, `DEFAULT_CHANNEL_FACTOR` = market→XU100/fx→USDTRY/rates_cds→TRCDS5Y/energy→BRENT/foreign_flow→FFLOW/holding→XHOLD; **'sector' intentionally unmapped** = name-specific, so sector-targeting events are surfaced-as-skipped, not mapped to one index); runs a **grid** of differential-exposure variants (quantile×drift_window×exposure_lag) = the honest `n_trials`; **purged walk-forward OOS selection**; the full M4 gate over 3 books + capacity; and the **§240 channel-stress second output** (`_channel_stress_report`: each event type's prior shock re-priced through the latest exposure tensor → worst-exposed names, inferred tier). Inputs are **injectable** so the self-test runs with no L2/network. Verdict → `signal_registry` when `write_l2`.
**Result:** verify **GREEN — 392 passed / 2 skipped** (+2 runner self-tests). Exit-gate self-test (`tests/events/test_run_event_signal.py`): a planted **concentrated entry-day** cross-sectional drift is **promoted** end-to-end (beats baselines + DSR passes + PBO low + venue-feasible), a shuffled-exposure **null is rejected**, empty inputs return a clean empty report.
**Decided:** the planted self-test world uses a **concentrated entry-day** move (not multi-day persistent) — a persistent drift is also caught by the **persistence baseline** (PBO→0.5, dominance is a coin-flip), so the cross-sectional edge must live in exposure, not time-series autocorrelation. The differential-exposure signal needs **no walk-forward variant module of its own** — the grid is over design choices (quantile/window/lag), reusing the M5 walk-forward shape.
**Open:** the runner is proven on synthetic; the **M6 exit gate has NOT been RUN on real data** because the **Turkey GKG backfill into the real L2 is not done** (the gate needs real `events`+`event_targets` over the `betas` window). LLM per-event `event_targets` (magnitudes, `gdelt_llm`) still unbuilt — the runner currently uses the inferred-tier prior seed. No `scripts/ingest_gdelt.py` driver yet.
**Next action (NEEDS A USER DECISION — surfaced):** the one-time **Turkey GKG backfill** is the blocker, and GKG files are **~5.3 MB each × 96/day** ⇒ a 2023–2026 backfill is ~600 GB / many hours. Decide the **backfill window/cadence** (full window vs shock-year vs thinned cadence vs Events-table 98 KB/file bulk) before kicking it off; then run the gate via a new `scripts/ingest_gdelt.py` + `run_m6_event_signal(store, as_of=…)`.

---

## 2026-06-25 (cont., live) — M6: GDELT live ingestion path proven end-to-end → L2 + PIT (milestone M6 🟢 in progress)
**Did:** Took the offline adapter live (the network half is ingestion-layer, §4-sanctioned) and built the L2 landing path.
- **Live reachability + real-data parse:** GDELT raw feed is up (`lastupdate.txt` → newest GKG file). Ran the offline parser against a **real** 15-min GKG file (1277 records) — column layout, Turkey filter, and theme classifier all work unmodified; 17 Turkey → 14 typed (armed_conflict/terror/diplomatic/cbrt/natural_disaster), skips counted. **The offline scaffolding is validated against production data.**
- **Captured the smoke golden:** `tests/golden/gdelt/gkg_smoke.json` — a REAL, immutable historical slice (2025-03-19 09:30, the project reference shock date): **26 Turkey records** (stable on re-fetch). `GdeltAdapter.smoke_check()` **PASSES live** (no drift); `test_gdelt.py::test_live_smoke_check_or_skip` now RUNS (not skips) and passes.
- **Built the L2 landing path** in `ingest/pipeline.py`: `_land_gdelt_records` (offline-testable: GKG records → `gkg_records_to_l2_rows` → PK-idempotent `write_parquet` to `events`+`event_targets` + §4 audit report) and `ingest_gdelt_events(adapter, store, start, end)` (the network orchestrator). Added `event_date` to `_DATE_COLS`.
- **Offline landing tests** (`tests/ingest/test_gdelt_landing.py`, 4): only-typed-Turkey written + skip counts, inferred-tier `event_targets` provenance (sign-only, `taxonomy_prior`), PK-idempotent re-land, and **the PIT keystone** — a `PITAccess` read dated 2025-03-18 sees nothing, 2025-03-19 sees all three (no look-ahead, §5).
- **Live end-to-end proof:** fetched a bounded 1-hour real window (2025-03-19 09:00–09:45) → **109 Turkey records → 68 typed events + 203 target rows** (41 untyped skipped) → landed to a throwaway L2 → read back through `PITAccess` (0 before 03-19, 68 on 03-19). All 11 event types appear across the window. The full **live → L2 → PIT** path works on real data.
**Result:** verify **GREEN — 390 passed / 1 skipped** (live tests run with the feed up). Only the throwaway L2 (scratchpad) was written — **the project L2 store was NOT touched**; no production backfill yet.
**Decided:** smoke golden = an **immutable historical 15-min file** (not the live tip) → a stable value anchor. The GKG-theme→type classifier needs no change for real data (the offline patterns hold).
**Open:** (1) **The one-time Turkey backfill is NOT run** — `ingest_gdelt_events` over the betas window (≈2023–2026 to match L2 `betas`) is a long crawl (~96 files/day × ~5MB); run it as a **background job** into the real L2. (2) LLM per-event `event_targets` (magnitudes, `gdelt_llm` tier) still unbuilt. (3) **Real-data M6 runner** (`events/run_event_signal.py`) + the **M6 exit gate run** (venue-feasible spread clears M4 + control-name survival) — the actual milestone completion — still ahead. (4) Per-doc events (no cross-doc dedup yet). (5) `short_eligible` empty (M2 Matriks blocker).
**Next action:** kick off the **one-time Turkey GKG backfill** into the real L2 (`scripts/ingest_gdelt.py` over the betas window, resilient/idempotent, background), then build `events/run_event_signal.py` and **RUN the M6 exit gate**.

---

## 2026-06-25 — M6: GDELT ingestion design locked (raw feed, NOT BigQuery) + offline adapter scaffolding (milestone M6 🟢 in progress)
**Did:** Two things — a sourcing decision and the offline half of the ingestion adapter.
- **Read both GDELT codebooks** (Event V2.0, GKG V2.1; extracted via pypdf) and answered the user's question: **Google Cloud / BigQuery is NOT the only way and is the wrong fit here.** GDELT has three access paths — raw 15-min file feed (free, no account, plain HTTP), DOC/GEO REST APIs (search, not bulk), and BigQuery (GCP account + billing). The codebooks describe a flat tab-delimited CSV; BigQuery is just a hosted layer on top. **Decision: raw FILE feed.** §4 forbids a live/mutable remote in the reproducible path, and we only need the tiny **Turkey slice**, so BigQuery's planet-scale value doesn't apply. Confirmed the BigQuery lean lives **only** in `data-sourcing-v2.md` (lines 42/82/136/194/197) — the *built* M6 code (the `events`/`event_targets` schema) is **source-agnostic**, so the raw-feed rows land with ZERO rework. (Doc to be corrected when ingestion finalizes; flagged, not silently edited.)
- **User forks chosen (both recommended):** ① primary stream = **GKG** (themes map onto the 11 §234 types better than verb-based CAMEO; richer for LLM channel extraction); ② **raw-feed full backfill** (deterministic 15-min URLs → Parquet cache, self-contained, no GCP).
- **Built `tmkg/ingest/gdelt.py`** (offline half, mirrors `FredAdapter`): pure helpers — `gkg_15min_urls`/`gkg_url` (deterministic, no master-list download), `parse_gkg_csv` (27-col GKG 2.1 layout; drops truncated lines, raises `ContractDrift` on a non-empty file that yields nothing — never silent-empty §4), `location_country_codes`/`is_turkey_record` (FIPS **TU**, not CAMEO TUR), `classify_event_type` (GKG-theme→type, a tunable inferred-tier prior `GKG_TYPE_THEME_PATTERNS` scored + `GKG_TYPE_PRIORITY` tie-break; `None`=untyped→skip-and-count), `parse_tone`+`modeled_severity` (0..1 from |tone|, saturates at 10, **NOT price-derived** §5; NULL when tone absent), `to_event_row` (PIT collapse: `event_date=knowledge_date=V2.1DATE` publication date), `to_event_target_rows` (seeds from `taxonomy.TYPE_CHANNEL_PRIOR`, `evidence_tier='inferred'`, `source='taxonomy_prior'`, sign-only — LLM `gdelt_llm` override is the live step), `gkg_records_to_l2_rows` (confidence-tiered: writes typed-Turkey events, counts skips). Network half (`fetch` over [start,end] via httpx+zipfile, 404=skip-a-gap not fabricate, `smoke_check` against a yet-to-capture golden) written but **untested-live** — the live pull is a [STOP] §8 session.
- **Fixture + tests:** `fixtures/gdelt/gkg_sample.csv` (clearly-labelled ILLUSTRATIVE synthetic GKG, README says never-L2 §4) + `tests/ingest/test_gdelt.py` (20 offline + 1 live-skip): URL cadence, parse/drop/drift, Turkey filter, classifier + priority tie-break + **full-type coverage** assertion, severity saturation/NULL, the PIT date collapse, inferred-tier targets (cbrt→rates_cds +1 / fx −1), and the skip-counting assembler.
- **Strengthened the §4 invariant:** added `tmkg.ingest.gdelt` to `_NET_ADAPTER_MODULES` in `test_no_network_in_signal_layer.py` — the future `events` runner (an L3-scanned package) provably can't import the network-bearing adapter.
**Result:** `make verify` **GREEN — 382 passed / 5 skipped** (+18 net over M6 foundation's 364). No network touched; nothing landed in L2 (offline scaffolding only).
**Decided:** GDELT = **raw file feed, not BigQuery** (no ADR — a sourcing/implementation choice faithful to §4; recorded here + in memory). GKG-themes primary + raw-feed full backfill (user forks). The GKG-theme→type map is a **tunable inferred-tier prior** (like `TYPE_CHANNEL_PRIOR`), not a verified mapping.
**Open:** (1) **Network half is the next live [STOP] session** — capture a REAL 15-min GKG slice as `tests/golden/gdelt/gkg_smoke.json` (the smoke golden must NOT be fabricated, §4), then a one-time Turkey backfill → Parquet/L2 `events`+`event_targets`. (2) **LLM per-event `event_targets` extraction** (`gdelt_llm` tier, overrides the prior with magnitudes) not built. (3) **Real-data M6 runner** (`events/run_event_signal.py` + `scripts/`): PIT-read `events`/`event_targets`+`betas` → differential-exposure weights → M4 gate over 3 books (the **venue-feasible** survival is the real M6 exit gate) → channel-stress report → `signal_registry`. (4) M6 exit gate **not run on real data** ("spread clears M4" + "control names survive a typical event" still unmeasured). (5) Per-doc events (no cross-doc clustering/dedup yet) — a documented downstream refinement. (6) `short_eligible` still empty (M2 Matriks blocker) ⇒ events targeting `foreign_flow`/shorts get a thin venue test until it lands. (7) `data-sourcing-v2.md` BigQuery lines to correct.
**Next action:** the **live GDELT ingestion session** ([STOP] §8) — run `GdeltAdapter.fetch` against the real feed for a known window, capture the smoke golden, do the one-time Turkey backfill to L2, then build the `events/run_event_signal.py` runner and RUN the M6 exit gate.

---

## 2026-06-24 — M6 event engine: data-model + channel-stress foundation (milestone M6 🟢 in progress)
**Did:** opened M6 (geopolitical event engine) with the offline, no-network foundation — the spine + the "second output", before the alpha signal and before any live GDELT pull (same posture as M4: build the machinery before the live data). Three slices, repo green between each:
- **L2 schema** (`l2/schema.sql`): two bitemporal tables. `events` (event_id, event_date, **date_precision** day|week|month|quarter, event_type, actors, geography, severity 0..1, source, knowledge_date) — the L2 projection of the L1 `Event`; `knowledge_date` = publication so a PIT read never sees an event before it was reported. `event_targets` (the `TARGETS` edge, **modeled**): one row per (event, channel) with `shock_sign`, `shock_magnitude`, and the full §5 soft-edge provenance quartet — `confidence`, `evidence_tier ∈ {verified,inferred}`, `uncertainty`, `source`. Added both to the `PITAccess` allow-list and to `test_l2_roundtrip` EXPECTED_TABLES.
- **`tmkg/events/taxonomy.py`** — the shared vocabulary, pure data: the **11 §234 event types**; **CHANNELS ≡ the factor-ladder roles** (`neutralize.DEFAULT_LADDER`) — the decisive reuse claim, since a name's *exposure* to a channel is exactly its M2 beta to that rung's factor; and `TYPE_CHANNEL_PRIOR`, a coarse **inferred-tier** signed type→channel default (fallback `TARGETS` + stress seed, overridden by per-event extraction, never promoted to verified). `validate()` + `prior_shock_vector()`. Tests (7) pin the 11-category count, the channels==ladder-roles identity, and that `validate()` catches a bad sign / unknown channel.
- **`tmkg/events/channel_stress.py`** — the §240 **second output** (risk spine, not alpha): `channel_stress_pnl(exposures, shock, weights=)` re-prices the book via one dot of each name's exposure row with a signed channel-shock vector → per-name stress return + worst/best-exposed + portfolio P&L; `shock_from_prior(type, severity)` turns the taxonomy prior into a scenario. §4 honesty rails: NaN exposure ⇒ 0 contribution **and** lowered reported coverage (no fabricated full-coverage zero), an unmodelled shock channel is **surfaced** not dropped, an empty channel intersection **raises** rather than returning a resilient-looking zero. Tests (8) reconcile per-name + portfolio P&L **to the penny** (the M6 exit-gate "stress reconciles against a hand-checked shock") + the three honesty rails.
- **`tmkg/events/differential_exposure.py`** — the §236 **alpha**: cross-sectional differential exposure (not single-name event studies — events cluster in Turkey). `event_cross_section_weights` longs the top-quantile names by shock-aligned exposure / shorts the bottom (dollar-neutral unit, NaN exposure dropped not zeroed, no-dispersion ⇒ flat); `differential_exposure_weights` assembles the (date×symbol) panel by holding each event's spread over its post-event **drift window** using **lagged** (PIT) exposure, superposing overlaps then renormalising. The §238 caveat is first-class: `control_survival` counts low-|exposure| **control** names by an **absolute** threshold (a quantile would always return the same fraction and hide the whole-tape-shock failure) → a thin `control_fraction` is a down-weight, never fabricated dispersion. Tests (10) pin the spread (hand-checked), sign-flip, no-dispersion/NaN safety, the control guard incl. the whole-tape-no-controls case, the zero-outside-drift PIT hold, and the **exit-gate self-test through the M4 gate**: a planted exposure-drift world is **promoted** (beats the baseline ladder + DSR passes + PBO low) while a shuffled-exposure null is **rejected** (the move concentrated at entry + symmetric betas means momentum/own-event/market-beta baselines can't recover it).
- The new `events` package is now under the **no-network L3 AST scan** (`_L3_PACKAGES += "events"`).
**Result:** `make verify` **GREEN — 364 passed / 2 skipped** (+27 over M5: 7 taxonomy + 8 channel-stress + 10 differential-exposure + 2 live tests that ran with network). No network touched by the build; nothing landed in L2 yet (schema only — real events come from the GDELT slice).
**Decided:** **a "channel" is a factor-ladder role** — the event engine reuses the M2 `betas` exposure tensor rather than inventing a parallel exposure layer (so `TARGETS`/stress route through the same rungs the residual machine strips). Geography is a §119 TARGETS target with no geography *factor* in L2 yet → geo-incident events route through proxy factors (energy/fx) for now; surfaced as a known gap, not fabricated. Not consequential enough for an ADR (no design deviation) — captured here.
**Open:** (1) **GDELT ingestion adapter** not built (`ingest/gdelt.py`, network-gated: offline-testable parser + live `smoke_check` + golden + audit report; LLM/curated extraction of per-event `event_targets` from the raw feed) — a live session, like the still-pending M2 factor ingestion. (2) **Real-data M6 runner** not built (`events/run_event_signal.py` + `scripts/`): PIT-read `events`/`event_targets` + `betas` (exposure-per-channel panels), build the differential-exposure weights, judge through the M4 gate across the 3 books (the **venue-feasible** survival is the real M6 exit gate, not the frictionless self-test), emit the channel-stress report, land the verdict into `signal_registry`. (3) Matriks foreign-custodian list still blocks `short_eligible`/`foreign_flow` (carried from M2/M5) — events that target `foreign_flow` will have a thin venue/short test until it lands. (4) Geography channel still proxied (no geo-factor in L2). (5) the M6 **exit gate is not yet RUN on real data** — the offline machinery (taxonomy + stress + diff-exposure + self-test) is complete and GREEN, but "spread clears M4 in the venue-feasible book" + "control names survive a typical event" must be measured on ingested GDELT events before M6 is COMPLETE.
**Next action:** build the **GDELT ingestion adapter** (`tmkg/ingest/gdelt.py`) — the one network-touching M6 piece: pull the raw GDELT event stream for Turkey, parse to `events`-schema rows (event_date + precision, type via taxonomy, severity), with an offline parser unit-test + a live `smoke_check` against a committed golden + a §4 audit report; then the per-event `event_targets` mapping (LLM/curated, inferred-tier) and the real-data runner. Treat the live pull as a [STOP]-style live session per §8 if the source contract surprises.

## 2026-06-23 — M5 first real signal: residual stat-arb → NO-GO through the venue-feasible book (milestone M5 ✅ COMPLETE)
**Did:** built and ran the first *real* signal through the M4 judge. Two new L3 modules (each shipped with tests, repo green between slices):
- `tmkg/signals/statarb.py` — **peer-relative residual mean reversion.** From the M3 surviving residual-corr edges, a comove-predicted residual `r̂_i = Σ_j ρ_ij·r_j / Σ_j|ρ_ij|` (signed); the dislocation `s_i = r_i − r̂_i` is the residual *in excess of linked peers*; fade an accumulated dislocation z-score (`−z(Σ s)`), `shift(1)` for PIT — uses the *graph* so it can beat the naïve own-name reversion (M4 baseline (c)). `accum_window` axis controls turnover (the cost-survival lever). Rolling edge refit reuses the M3 `correlation` primitives. Unit tests (5): hand-checked `edge_matrix`/`comove_predicted`, peerless⇒NaN (no fabricated 0), no-edges⇒flat book, shift-no-lookahead, **peer-relative provably beats own-name reversion** on a synthetic world.
- `tmkg/signals/run_statarb.py` — the M5 runner. PIT-reads residuals + total_returns (+limit-lock); liquid sub-universe by residual coverage; honest **12-variant grid** = `n_trials`; **purged walk-forward OOS selection** (wires `purged_walk_forward_splits` into a real train/test split — the carried M4 D2/W1 entry condition); full promotion gate over 3 books + capacity curve; lands filtered `residual_corr` snapshot + `signal_registry` verdict + JSON report. `scripts/run_m5_statarb.py`.
- M5 exit-gate self-test (`tests/signals/test_m5_exit_gate.py`, 2): a strong low-turnover synthetic edge is **promoted** end-to-end (venue net>0, DSR passes, registry round-trips through PITAccess, snapshot lands); shuffled-label null **rejected**.
**Result:** **NO-GO (ADR-0004).** Real run as_of 2026-06-15, 200 liquid names, 505 OOS dates, n_trials=12. Net Sharpe research **+0.074** / venue **−0.052** / stress **−0.038**. Candidate **beats the baseline ladder** (−0.052 > best baseline `differential_exposure` −0.073) and **PBO 0.40**, but **DSR ≈ 0** + venue net negative ⇒ fails capacity floor ⇒ **not promoted**. Robust: *every* in-sample variant is +research/−venue; best turnover-efficient variant tops out at −0.026 venue; longer horizons don't cross break-even (the frictionless edge itself decays — reversal is genuinely short-horizon). A small genuine frictionless edge **too thin to trade net of 10 bps + borrow.** Landed: 92 FDR+sector-restricted survivor edges to L2 `residual_corr` (sensible: ANSGR-TURSG insurance, AKFGY-VKGYO REIT, BIMAS-SOKM retail); NO-GO row to `signal_registry`. `make verify` **GREEN — 337 passed / 4 skipped** (+4 over M4; 7 new tests, 3 pre-existing live skips vary). Cost model NOT weakened (§8).
**Decided:** **M5 = NO-GO (ADR-0004)** — the correlation pillar yields a clean residual substrate but no standalone tradable alpha at daily/weekly frequency; do not manufacture a pass by softening costs. The L2 `residual_corr` table (allow-listed, "filtered snapshot never dense") is the design's landing spot — used; an L1 Kuzu `RESIDUAL_CORR` rel-table projection + `LEAD_LAG` are deferred (no edge needs them yet).
**Open:** (1) `short_eligible` **empty** (M2 blocked on the Matriks foreign-custodian list) ⇒ venue book can't police per-name short bans ⇒ **stress book is the binding short test** until it lands. (2) Per VERIFICATION §4, a project-level exit gate invites a fresh-agent adversarial falsification pass — not yet run for M5 (the verdict is a *rejection*, lower lookahead-risk, but the runner wiring/edge-leak deserves a look if M5 is ever revisited for a GO). (3) Revisit avenues in ADR-0004 §Consequences if the correlation pillar is reopened.
**Next action:** begin **M6 — geopolitical event engine** (next pillar): `Event` ingestion (GDELT + taxonomy) with date precision + `TARGETS` channel mapping; cross-sectional differential-exposure spread signal through the M4 gate; channel-stress P&L. Carry the `residual_corr` snapshot as a reusable input.

## 2026-06-23 — M4 adversarial review (VERIFICATION §4) → 2 defects fixed, dispositioned (milestone M4)
**Did:** ran the §4 adversarial review — a fresh falsifying agent (no build context) attacked the M4 harness with the sole mandate of making it lie. It confirmed the statistical core (DSR/PSR/E[max] match Bailey–LdP to machine precision; PBO CSCV correct) and the P&L arithmetic (incl. limit-lock-carry + borrow) are sound, and that degenerate/known-good/textbook-overfit cases behave. It found **two real defects in the *trust boundaries***, now fixed in `signals/promotion.py` with regression tests in `tests/signals/test_adversarial.py` (6):
- **D1 (critical) — the data-mining haircut was opt-in and OFF by default.** `evaluate_candidate(n_trials=1)` was the default, and `expected_max_sharpe(V, 1)=0` ⇒ DSR collapsed to plain PSR-vs-0 ⇒ the trial-count protection was inert; the agent mined 200–2000 random weight vectors over pure noise and got the in-sample winner **PROMOTED 17–19/20 seeds**. PBO didn't catch it because it only ranked the winner against the 3 fixed baselines, never its mining siblings. **Fix:** `n_trials` is now a **required** keyword (raises if omitted/<1) — it can no longer be silently disarmed; and a new `trial_pnls` arg lets a caller pass the mined family so (a) the cross-trial Sharpe dispersion drives the DSR variance (the correct LdP input, not the single-strategy floor) and (b) every sibling enters the PBO set so selection bias is visible. With honest `n_trials` the mined noise winner is now rejected (pinned by `test_mined_noise_winner_is_rejected_when_trial_count_is_honest`, frictionless research book — no cost help).
- **D3 (low) — short history raised `ValueError` instead of failing cleanly.** `T < pbo_partitions` propagated an exception out of the gate. **Fix:** `_safe_pbo` clamps partitions to the largest even ≤ T and returns a NaN verdict when T<2 ⇒ the candidate fails `pbo_below_threshold` (rejected), never crashes.
- **D2 (high) — DISPOSITIONED, not code-fixed (by design + M5 wiring).** The gate has no *internal* lookahead guard and `purged_walk_forward_splits` is built but unused. Lookahead in THIS project is a **data-layer invariant** enforced by `tmkg.pit.PITAccess` (signal code reads only knowledge_date ≤ as_of), not the backtester — the gate trusts pre-aligned PIT-correct weights, now stated explicitly in the `evaluate_candidate` docstring. Wiring purge/embargo CV into the evaluation is an **M5 task** (it bites when real signals are fit/validated with train/test splits). Logged as the M5 entry condition.
- **W1/W2/W3/W4 — noted, lower severity.** W1 (coarse PBO with only 4 strategies) is mitigated by D1's `trial_pnls`. W2 (missing/NaN fwd returns earn 0 — a survivorship-masking risk per §5) and W3 (`capacity_floor` defaults 0) carried to M5 (the M5 exit gate already mandates a *stated* capacity floor). W4 (variance fallback is a proxy) resolved when `trial_pnls` is passed.
**Result:** `make verify` **GREEN — 333 passed / 1 skipped** (+6 over the build). Self-test still green with the now-mandatory `n_trials`.
**Decided:** the adversarial pass is **clean / dispositioned** — D1+D3 fixed with regression tests, D2 is a documented design boundary + M5 task. Per VERIFICATION §4 the M4 harness gate is now satisfied.
**Open:** M4 is functionally COMPLETE; the only remaining items are the M5 entry conditions (wire purge/embargo for real train/test, real-data PIT runner, survivorship-NaN guard, stated capacity floor).
**Next action:** begin **M5 — first real signal (residual stat-arb)**: build candidate weights from the surviving M3 persistent-core residual edges, feed through this gate with an honest `n_trials` + `trial_pnls`, require survival in the **venue-feasible** book (per the M3 caveat).

## 2026-06-23 — M4 judge BUILT + exit-gate self-test GREEN (milestone M4 🟢 → adversarial review pending, then M5)
**Did:** built the entire M4 scoring harness ("build the judge before the contestants") as four L3 modules, each shipped with tests, repo green between slices:
- **`signals/stats.py`** — the anti-overfit core: per-period Sharpe, Probabilistic Sharpe (Bailey–LdP), **Deflated Sharpe Ratio** (PSR at the expected-max Sharpe of `n_trials` — the trial-count haircut), and **PBO** via Combinatorially Symmetric Cross-Validation. Pure NumPy/SciPy. `tests/signals/test_stats.py` (17) pins the decisive asymmetry: DSR fails a null / passes a skilled strategy; PBO = 1.0 on a sign-flip overfit, 0.0 on a real edge, ~0.5 on noise.
- **`signals/backtest.py`** — PIT backtester: `purged_walk_forward_splits` (causal, purge+embargo), `CostModel` (per-name turnover cost + annualized borrow), three `BookConfig`s (**research** frictionless / **venue_feasible** enforces `short_eligible` + limit-lock carry + costs / **stress** blanket short-ban + 3× crowding cost), and a `capacity_curve`. `tests/signals/test_backtest.py` (11) incl. a **hand-checked toy P&L reconciled to the penny** in research + venue-feasible.
- **`signals/promotion.py`** — the **naïve-baseline ladder** (persistence/momentum · sector+FX differential-exposure · sparse own-factor event-study) + `evaluate_candidate`, the promotion gate = AND of {beats every baseline net Sharpe · clears capacity floor in venue-feasible · DSR passes · PBO < threshold}.
- **`signals/registry.py`** — bitemporal write of each verdict into L2 `signal_registry` (append-only, PK-idempotent) + a §4 JSON audit report.
**Result:** **`make verify` GREEN — 327 passed / 1 skipped** (was 292; +35 tests). The no-network AST scan stays green (all four new modules are L3-clean). **M4 exit-gate self-test (`tests/signals/test_harness_selftest.py`, 4) passes:** known-null (shuffled labels) → DSR 0.03, **rejected** on all three checks; known-good toy → net Sharpe 1.19 vs baseline 0.66, DSR 1.000, PBO 0.000, **promoted**; all three books emit; verdict round-trips through L2 + PITAccess (pre-write read sees nothing — no lookahead). Margins are large, not marginal.
**Decided:** **"`DSR > 0` after trial-count adjustment" is implemented as DSR (a probability ∈ [0,1]) ≥ a documented confidence (default 0.95)** — gating on a literal ">0 probability" is meaningless; 0.95 is the faithful reading of "credibly positive deflated excess Sharpe". Documented in `stats.py::DSR_CONFIDENCE`; **not** a weakened invariant (no prior test existed). PBO is computed over {candidate, *baselines} (CSCV). Not consequential enough for an ADR — captured here + in BUILD_PLAN M4 notes.
**Open:** (1) **Adversarial review of the harness is still owed** (VERIFICATION §4 lists the M4 harness self-test as a [STOP]-style gate) — a fresh agent should try to make the gate promote noise / reject a real edge before M4 is declared COMPLETE. (2) No PIT *runner* yet that feeds **real** M2/M3 residual panels into the gate — folds naturally into M5. (3) The toy net Sharpes are per-period and high by design (clean synthetic predictor) — appropriate for a self-test, not a real-data claim.
**Next action:** surface the M4 harness to the user for the **VERIFICATION §4 adversarial review** (spawn a fresh falsifying agent); on a clean pass, begin **M5** (residual stat-arb from the surviving M3 edges → through this gate → venue-feasible book).

## 2026-06-23 — M3 residual-survival [STOP] gate RUN on wide universe → **GO (with caveat)** (milestone M3 ✅ → M4)
**Did:** Matriks recovered (the 504 outage that blocked the prior session is gone — confirmed by pulling real bars; `symbolSearch`/`fundamentalAnalysis` still flap an intermittent auth-500, but the universe driver needs neither). Ran the full M3 verdict end-to-end:
- **Ruled out a feared contract drift (false alarm).** My manual `historicalData` probes returned downsampled `sampledBars` (30 of 864 bars) — but `pipeline._fetch_bars` already passes **`rawBars=True`**, which returns the full `allBars` series at 100% (`method=RAW_LIMITED`); the team had already hardened against exactly this. `limit` caps at 1000, so the driver's 900-calendar-day chunking is correct. No code was broken.
- **Hardened the universe driver** (`scripts/ingest_universe.py`): `NO_DATA_FOUND` (gateway healthy, name has no series) no longer counts toward the 15-consecutive-failure §8 burst-abort — without this a cluster of dataless/delisted names could falsely abort a long run. New `_is_no_data()` + `tests/ingest/test_universe_driver.py` (6 tests).
- **Wide-universe ingestion** (`scripts/ingest_universe.py 2023-01-02 2026-06-23 2026-06-23`): **570/601 equity names** landed full ~865-bar USD total-return histories; 24 no-data + 7 persistent `SERVICE_ERROR` names logged-and-skipped (§4, never fabricated). universe_class: 452 operating / 53 holding / 48 gyo_reit / 17 investment_trust. **574 names** with `total_returns` in L2; all 19 registry factors present.
- **Clean M2 refit + M3 gate** (new `scripts/run_m3_gate.py`): deleted stale BIST-30 betas/residuals, refit `run_m2_factor_model(require_all_factors=True, window=60, min_obs=40)` over the full 18-rung ladder ending `…>FFLOW>XHOLD` → residuals for **573** names. Gate over 573 names / 48 sectors / 605 dates → `data/cache/m3_gate_report.json`.
- **Robustness sweep** (new `scripts/m3_robustness.py`): sector granularity (leaf-L2 57 blocks vs parent-L1 15) × window {90,120,150}d → `data/cache/m3_robustness_report.json`.
**Result:** **`make verify` GREEN — 295 passed / 1 skipped** (the lone skip is the M5 L1-provenance soft-edges; +9 net from 286/4 — my 6 driver tests + live drift tests now running since Matriks is up). **M3 gate coded verdict = NO-GO, but the decision = GO.** Headline (leaf, 120d): median **lift 19.6** (≥3 bar) across per-window pairs **17.7/25.0/21.5/10.5** incl. across the 2025-03-19 shock; median Jaccard **0.0961** (just under the 0.10 absolute bar — the *only* failing check). Robustness: **`lift` is unanimous 14–37× in all 6 cells**; coarsening sectors *lowers* Jaccard (0.095→0.05) while *raising* lift → absolute-Jaccard is **edge-count-confounded**, lift is the scale-invariant survival metric (the design's "125k pairs / chance-adjusted" framing).
**Decided (→ `decisions/ADR-0003`):** **M3 = GO.** Stable residual structure survives the full strip decisively and robustly; because residuals are ⊥ FFLOW by the M2 orthogonality invariant, this is *by construction* not the flow factor — the pillar's life-or-death thesis **holds**. The coded gate stays NO-GO (its NO-GO-biased all-must-pass rule is correct and was **not** weakened, §8); GO is a documented human go/no-go on reasoned grounds, **ratified by the user**, overriding the absolute-Jaccard sub-check as the wrong instrument at 573 names. The 30-name BIST-30 NO-GO is now confirmed a small-sample artifact.
**Open / caveat carried to M5:** absolute window-to-window edge overlap is moderate (~9.5%) and weight rank-stability is low (ρ 0.14, non-gating) — residual stat-arb baskets must be built from the **persistent core** and proven in the **venue-feasible** book, not frictionless research (the honest M5 exit gate). 7 names unservable by Matriks (`FIN/GLB/OYA/SEK/SKY/TSK/YKB` — persistent `SERVICE_ERROR`, not no-data); retry later. L2 wide-universe tables gitignored, regenerable via `ingest_universe.py` + `run_m3_gate.py`.
**Next action:** Begin **M4 — promotion gate + signal registry + PIT backtester** (the judge, built before M5 so no signal is graded by a harness written to flatter it): naïve-baseline ladder, signal registry with DSR/PBO + trial-count, PIT backtester with purge/embargo + per-name cost/borrow + capacity, three books (research / venue-feasible / stress). Exit gate: a known-null (shuffled-label) signal correctly fails, a known-good toy passes, toy P&L reconciles hand-checked.

## 2026-06-22 (night) — M3 residual-survival machinery BUILT + validated; BIST-30 dry-run = confounded NO-GO (milestone M3 [STOP], IN PROGRESS)
**Did:** Built the entire M3 [STOP]-gate machine in 4 vertical slices, all pure/L2-honest, plus unskipped the §4 signal-layer guard. The gate answers the pillar's life-or-death question: *does stable residual linkage survive the factor strip, or was the "alpha" just the flow factor we removed?*
- **Slice 1 — residual-correlation engine** (`src/tmkg/signals/correlation.py`, pure): `residual_panel` (long L2 residuals → ragged wide panel, drops <min_obs names — never zero-fills), `pairwise_correlation` (pairwise-complete sample corr + per-pair joint-obs count; <min_obs ⇒ NaN, never a measured zero), `shrunk_residual_correlation` (Ledoit–Wolf on the **standardized** residuals → inversion-safe correlation; refuses <min_obs complete-case rows — §201 never invert raw `p≈n` sample cov), `benjamini_hochberg` + `fdr_edges` (FDR control: 125k pairs guarantee false discoveries; t-stat p-values → BH; `candidate_pairs`/`min_abs_corr` filters). 14 tests incl. the known-good/known-null pair.
- **Slice 2 — sector restriction + topology** (same module): `within_sector_pairs` (the FDR candidate family — encodes the intra-sector prior + shrinks the testing burden), `sector_restricted_correlation` (**Alves-style** block-diagonal: LW within each sector, cross-sector exactly 0 — not estimated-then-thresholded), `mst_filter` (Mantegna `√(2(1−ρ))` MST) + `pmfg_filter` (planar-maximally-filtered graph, `3(N−2)` edge limit, contains the MST). 10 tests.
- **Slice 3 — rolling-window stability** (`src/tmkg/signals/stability.py`, pure): the headline measurement — per consecutive window pair, **edge-set Jaccard** of the FDR within-sector network vs a **random-overlap baseline** (`lift = jaccard/random_jaccard` = chance-adjusted persistence), plus weight rank-stability. `decide_gate` applies the documented conservative GO/NO-GO. 11 tests incl. **persistent-block → GO** and **structureless → NO-GO**.
- **Slice 4 — gate runner** (`src/tmkg/signals/gate.py`): the only L2-touching piece — reads `residuals` through `PITAccess`, builds the panel, runs the stability metric with a caller-supplied L1-graph sector map, emits a JSON verdict report. 4 tests (synthetic L2; GO/NO-GO/PIT-gate/empty).
- **§4 guard unskipped:** `test_no_network_in_signal_layer` now AST-scans the L3 packages (signals/factors/returns/analytics) for network client libs + the network-bearing ingest *adapter* modules (matriks/evds/fred/worldgovbonds/base) — allows `pipeline`'s L2 readers (which `diagnostics` legitimately uses). Negative-control verified (planted `import requests` trips it, names the file).
**Result:** **`make verify` GREEN — 286 passed / 4 skipped** (was 246/5; +25 net new tests; the lone remaining invariant skip is the M5+ L1 provenance soft-edges). **Real dry-run on the 30-name BIST-30 residuals (`data/cache/m3_gate_dryrun_bist30.json`):** all 30 names resolve to L1 sectors (banks=5, chemicals=4, holdings=4, metal-goods=3, +smaller); **decision = NO-GO**, but the *only* failing check is `lift_clears_chance` (median lift **1.74** vs 3.0; median Jaccard **0.43** ✓, rank-ρ **0.47** ✓, ~11.5 edges, 4 window pairs).
**Decided / the key finding (NOT a trustworthy verdict):** the BIST-30 NO-GO is a **small-sample artifact, not a falsification.** With only ~29 within-sector candidate pairs and 8–17 edges/window, you select a large fraction of the candidate family, so the random-overlap floor is structurally inflated and `lift` is suppressed *even when real structure persists* (Jaccard/rank-stability are actually healthy). The real M3 verdict needs the **wider universe (~500 names)** so within-sector blocks are large and the candidate family big enough for `lift` to discriminate (the design's "125k pairs" framing). Also decided: weight rank-stability is **reported, non-gating** — a homogeneous-but-stable block has tie-noise ranks and a low ρ; vetoing it would be wrong (survival = edge-set persistence beyond chance; rank-stability is an M5 signal-construction refinement).
**Open:** ① **THE prerequisite for the verdict:** wider-universe residuals — ingest prices/total_returns for the ~500-name universe (M1 pipeline, live Matriks) → M2 fit → residuals in L2. Substantial live session. ② Foreign-flow (FFLOW) is in the factor set but the BIST-30 residuals were neutralized with the M2 ladder — confirm the wider-universe fit strips FFLOW before the gate (the whole "was it just the flow factor?" question). ③ Possible refinement: flag in the report when the candidate family is too small for `lift` to discriminate (guard against the artifact we just hit).
**USER DECISION (this session):** chose **full ~500-name universe** (most rigorous). Built `scripts/ingest_universe.py` — batch-resilient driver landing prices/total_returns/universe_class for the **601 equity-traded names** (v1 graph: ticker+ISIN+sector); logs-and-skips a single unreachable/data-less name (recorded skip ≠ fabrication, §4/§6) but aborts on a 15-failure burst (§8). **BLOCKED at launch (§8 HARD STOP): Matriks gateway returns HTTP 504 Gateway Time-out across ALL endpoints** (historicalData AND symbolSearch) — nginx upstream timeout = server-side outage, not auth (would be 401/403) and not rate-limit (429). Verified with known-good names (EREGL/GARAN/AKBNK/THYAO/ASELS all 504). Committed the ready driver (`87b1fab`); machinery in `67ab0be`.
**Next action (BLOCKED on Matriks recovery):** the moment the gateway is back, run `PYTHONPATH=src python scripts/ingest_universe.py 2023-01-02 <today> <today>` (≈601 names × 2 chunks × 1.5s ≈ 45–90 min, resilient), then `run_m2_factor_model(store, symbols=<landed>, as_of, specs=registry.specs(), require_all_factors=True, window=60, min_obs=40)` (DELETE stale betas/residuals first for a clean refit), then `m3_residual_survival_gate(store, as_of, sectors=<graph_sector_resolver map>)` → write `data/cache/m3_gate_report.json` → document the GO/NO-GO and **surface it (STOP gate, §8)**. Until Matriks is up there is no further M3 progress to make; the BIST-30 read stands as preliminary (confounded NO-GO). **User confirmed: WAIT until tomorrow for the Matriks API to recover** (after I clarified the blocker is per-name PRICE data — the universe+sectors+sub-sectors are already sourced from the v1 graph and in hand; the gap is the ~570 new names' USD total-return series, which only the Matriks spine provides; §4 forbids proxying it). Open offline option the user raised: add **sub-sector granularity** to the within-sector restriction (`within_sector_pairs` / `graph_sector_resolver`) — finer, more homogeneous blocks but fewer pairs each; main-sector remains the default for the survival test. **Diagnostic (don't re-test): the 504 is NOT request-size — a 1-day historicalData request fails identically to a 900-day one, same ~10s, and the error flaps between `504 Gateway Time-out` and `500 INTERNAL_ERROR "Authentication failed"` regardless of window. EVDS (different provider) works fine concurrently → our network/creds are healthy (same key worked in the M2 session; correct `X-API-Key: user:key` form per ADR-0002). It is a server-side Matriks backend outage. Chunking smaller will not route around it — wait for recovery.**

## 2026-06-22 (eve) — BIST-30 prices ingested + M2 exit gate RUN → MET (milestone M2)
**Did:** Closed the last M2 gate input and ran the gate end-to-end. **(1) BIST-30 price sample** — new driver `scripts/ingest_bist30.py` landed daily `prices` for all 30 BIST-30 names (live Matriks REST, chunked ≤900d under the 1000-bar cap) + USD-primary `total_returns` built through PIT (FX=USDTRY already in L2; CPI not in this L2 so `ret_real_try` left NULL by design). Full ~863-return histories per name; `ret_usd` nulls are limit-lock-censored/stale days + recent listings (DSTKF 2025, ASTOR 2023) — correct §5, not gaps. Audit `data/cache/bist30_ingestion_report.json`. **(2) `universe_class` derivation** — new `ingest/universe.py`: derives class from the v1 graph **sector** via a rule table (`gyo_reit`/`holding`/`investment_trust`/`etf`, default `operating`) — *derived, not hardcoded* (§129); resolves the 4 share-class ticker variants (ISCTR→ISATR, VAKBN→TVB, YKBNK→YKB, KRDMD→KRDMA) via a documented id-alias; refuses (never guesses) an unresolved name. Landed `universe_membership` for bist_30 = **25 operating / 4 holding (KCHOL,SAHOL,SISE,TAVHL) / 1 gyo_reit (EKGYO)**, 0 refused. **(3) Ran `run_m2_factor_model(require_all_factors=True, window=60, min_obs=40)`** over 2023–2026 → betas + neutralized residuals for all 30, then `m2_gate_diagnostics` (`data/cache/m2_gate_report.json`).
**Found + fixed a real estimator bug (the session's core finding).** The first gate run produced a **market beta of ~5e-6** (and break_ratios <1). Cause: `_estimate_betas` shrank the **raw mixed-unit covariance** — FFLOW (USD-mn level, σ~249) and TRCDS5Y (bps, σ~9) dominated the ill-conditioned Ledoit–Wolf inverse and crushed the simple-return factors' betas to numerical noise. **Fix:** standardize regressors before shrinkage (LW now acts on the scale-free **correlation** matrix), map betas back to raw units — exactly equivariant for OLS, meaningful for LW. Market beta → sensible 0.33 (partial), USDTRY → −0.95. Regression test `test_mixed_scale_factors_do_not_crush_small_factor_betas` pins it. **(Gotcha:** `write_parquet` is PK-idempotent `ON CONFLICT DO NOTHING` — re-running did NOT overwrite the stale betas; had to `DELETE FROM betas/residuals` before re-fitting.)
**Strengthened the betas-break test (user-directed) → criterion now passes.** The full 18-factor **partial**-beta break read ~0.6 (collinearity: 6 sector indices ~ market, EUR/yields ~ USD/CDS → noisy marginals). Added `regime_break_on_subset` (re-fits betas on a **parsimonious low-collinearity set** XU100/USDTRY/TRCDS5Y = market/FX/credit, the shock's actual drivers) + a `peri_obs` param to `assess_regime_break` (compares only the betas **nearest the boundary**, isolating the discontinuity from the slow drift that inflates a long regime's noise floor). Wired both into `m2_gate_diagnostics` (`regime_break_primary`). Progression: full-panel ~0.6 → parsimonious full-regime ~0.9 → **parsimonious peri-shock: XU100 1.82, TRCDS5Y 1.43 (>1, clean break), USDTRY 0.84** (Δβ 1.19 but FX locally turbulent). Betas demonstrably break across 19-Mar-2025 on the primary exposures.
**Result:** **`make verify` GREEN — 246 passed / 2–5 skipped** (skip spread = live Matriks/EVDS/WGB drift tests auto-skipping after the heavy BIST-30 pull rate-limited the gateway; +13 net tests this session: 12 universe_class, +1 beta scale, +3 diagnostics, and the L2/golden suites run twice). **M2 EXIT GATE MET on all four criteria** (`m2_gate_report.json`): ① no factor dropped (require_all, missing=[]) ② residual orthogonality (structural invariant) ③ plausible variance share per class (operating R²≈0.67, holding ≈0.80, gyo_reit ≈0.66; 30 scored) ④ betas break across the shock (peri-shock primary: market 1.82, credit 1.43).
**Decided:** (a) **Beta estimator standardizes regressors before LW shrinkage** — correctness fix, not ADR-worthy (implementation faithful to §201's "shrink because p≈n"; raw-unit mixed-scale shrinkage was simply wrong). (b) **`universe_class` is derived from graph sector via a rule table**, refusing unresolved names — no ADR (identity/reference ingestion, mirrors `ingest_delisting` + `IdBridge` posture). (c) The **betas-break criterion is judged on the parsimonious peri-shock break**, not the full-panel partial-beta marginals (which are collinearity- and drift-confounded); both are reported. L2 `betas` stay the full-model betas the neutralization uses; the parsimonious betas are an in-memory diagnostic only.
**Open:** L2 store gitignored (`data/l2.duckdb`; regenerable via the two ingestion drivers). USDTRY beta-break ratio 0.84 is the one sub-unit primary factor — large absolute shift (Δβ 1.19) but high local FX volatility; not a concern (market+credit carry the criterion). M2 deferred dividend-reconciliation thread (from M1) still open, non-blocking. **This is a milestone gate — surfaced to user; not auto-advancing to M3 (the [STOP] residual-survival gate).**
**Next action:** With the user's go-ahead, close M2 in `BUILD_PLAN.md` (criterion→evidence table) and begin **M3 — residual-survival gate [STOP]**: does stable residual structure survive the factor strip? (the cheapest-to-kill thesis, BUILD_PLAN sequencing rule 1). The residuals are now landed in L2 for the 30-name sample; M3 needs the wider universe + the stability metric + a documented GO/NO-GO.

## 2026-06-22 (pm) — LIVE factor ingestion (16 series) + foreign-flow factor FFLOW built (milestone M2, IN PROGRESS)
**Did:** First real L2 factor ingestion. New registry-driven driver `scripts/ingest_factors.py` landed **14 core factor series** in L2 `factors` over 2023-01-02..2026-06-22 (~865–906 daily pts each): XU100, USDTRY, EURTRY, BRENT, GOLD, the 6 BIST sector indices, XHOLD (Matriks REST), + VIX, NATGAS (FRED). Chunked ≤900 calendar days (Matriks `historicalData` caps `limit` at 1000); idempotent via L2 `ON CONFLICT`; PIT-readable (spot-checked: XU100 14796, USDTRY 46.46, GOLD 4190 as_of today). Audit report `data/cache/factor_ingestion_report.json`.
**Registry corrected to live reality:** NATGAS retargeted Matriks→**FRED `DHHNGSP`** (Henry Hub; "NATGAS"/8 variants are not Matriks symbols). **MSCIEM→`BLOCKED`** (EEM not on Matriks historicalData/symbolSearch, not on FRED; market rung covered by XU100+VIX). `blocked_factors()` now {FFLOW, MSCIEM}; tests updated.
**Foreign-flow probe + DECISION (the §8 fork):** empirically established no source is precise+daily+deep+foreign-split — `settlementAnalysis` per-custodian takas is **lossy presentation TEXT** ("188.52mn lot"; §4 fabrication risk on a diffed series); `historicalData` takas `tV_series` is precise+deep but **aggregate-only**; `institutionalFlow` broker-netting is precise but **recent-only ~2025+**; **EVDS weekly non-resident** is precise+official+**deep** but **weekly**. **USER CHOSE (2026-06-22): EVDS weekly non-resident = deep PRIMARY (resampled), institutionalFlow daily broker-netting = ~2025+ overlay/cross-check.** Clean §4 integrity.
**Result:** verify GREEN **220 passed / 2 skipped**. L2 store gitignored (`data/l2*`; regenerable); audit reports tracked. Commits `3d6345c` (ingestion) + earlier `90c07cc`/`1e1ca55` (foreign-custody reference+loader).
**FFLOW BUILT + INGESTED (same session, commit `8236f90`).** User supplied the datagroup series list; decoded the hierarchy: **`TP.MKNETHAR.M7`** = "2.1.1 Hisse Senedi" net değişim = weekly non-resident net equity FLOW (USD mn); **`TP.MKNETHAR.M1`** = the stock level. Verified across 2025-03-19 (M1 $35.8bn→$28.8bn that week; M7 +480→−444/−652) — unmistakable foreign-flow signature. Built: `series.LEVEL` (a flow is already an innovation → ret=value, not diffed); `EvdsAdapter.parse_weekly_series` (PIT `knowledge_date = Friday + 6d` release lag); `pipeline.ingest_foreign_flow` (lands FFLOW + FFLOW_STOCK as TRUE weekly obs — no calendar ffill in L2, §4); `build_factor_return_panel` now ffills weekly factors onto the daily grid aligned on **knowledge_date** (value steps on the Thursday release — no look-ahead, confirmed: 898 daily FFLOW rows). Registry FFLOW blocked→**available** (`evds`/`TP.MKNETHAR.M7`/LEVEL/`frequency=weekly`); driver ingests it. **16 series now in L2.** +5 tests, verify GREEN **225/2**.
**TRCDS5Y BUILT (commit `f56747f`).** New `WorldGovBondsAdapter` hits WGB's own public chart JSON API (`/wp-json/common/v1/historical`, POST FUNCTION=CDS Turkey(13)/60mo; **needs an `Origin` header** or 403) — structured, not HTML scrape; W3-sanctioned (not Investing.com/ToS). Daily Turkey 5y CDS in bps back to 2015; **1187 pts ingested 2023–2026**, spikes to ~300bps on 2025-03-24 (the shock). knowledge_date=bar_date (EOD), method=DIFF; golden anchors + smoke_check value guard. **17 factor series now in L2.** verify GREEN 232/2.
**RATES/CDS RUNG COMPLETE (commit `24861b1`).** WGB serves Turkey 2y/10y bond yields via the same endpoint (FUNCTION=Bond, DURATA=24/120) — so **all three** rates/CDS factors come from the one WGB adapter; the EVDS yield-code hunt was avoided (couldn't locate it; WGB is cleaner + consistent with CDS — deviation from data-sourcing-v2's EVDS-yield preference, justified). Generalized: `WGB_FACTORS` config map, `parse_cds`→`parse_series`, `ingest_cds`→`ingest_wgb_factor`. TRY2Y/TRY10Y = 1268 daily pts each, spike across 2025-03-19 (2y 35→50%, 10y 26→33%). **19 factor series in L2** — every ladder rung sourced except MSCIEM (market rung covered by XU100+VIX). verify GREEN 233/2.
**Open / remaining for the FULL gate:**
- **No stock prices/total_returns in this L2 yet** — THE remaining blocker. Gate needs per-name USD total returns (M1 `ingest_prices`→`build_total_returns`) for the name universe (or a BIST-30/50 sample). L2 currently holds factors only.
- AKD daily foreign-flow overlay (~2025+ cross-check leg) still unbuilt (secondary, not gate-blocking).
**Next action:** ingest prices+total_returns for a BIST-30/50 sample (M1 pipeline, USD-primary via USDTRY); then `run_m2_factor_model(require_all_factors=True)` over 2024–2025 → `m2_gate_diagnostics` → M2 exit gate (betas break 2025-03-19, plausible variance share per `universe_class`).

## 2026-06-22 — Q1 foreign-custodian list RESOLVED (self-sourced); Q2/Q3 answered (milestone M2, IN PROGRESS)
**Context:** Matriks customer relations came back EMPTY on the three PENDING questions — they only said "it's historical investor data, but there might be limits." User asked me to answer them myself by other means. Did so via the **interactive Matriks MCP tools** (symbolSearch/institutionalFlow/settlementAnalysis — query-only, not the pipeline) + public MKK/Takasbank sources. No pipeline code touched; this was investigation + a committed reference artifact.
**Did:**
- Pulled the **full live Matriks member roster** (`symbolSearch operation=metaMembers`, 1116 members) and the **MKK 'Aracı Kurum Üye Listesi' xlsx** + Takasbank Kurum Kodları. Discovered the actual classification mechanism: MKK segregates holdings into separate member codes by **account purpose** — `(YABANCI)` = non-resident/foreign custody, `(PORTFOY SAKLAMA)` = domestic custody. An institution carries several codes at once.
- Probed **history depth** with settlementAnalysis (takas) and institutionalFlow historic (AKD) across 2011→2025.
- Wrote the curated reference **`data/reference/foreign_custody_codes.json`** (repo schema) — the artifact the prior log called for.
**Result (answers):**
- **Q1 (the blocker) — RESOLVED.** The authoritative custody-based foreign set = the 6 `(YABANCI)` custodian codes: **CIY** (Citibank), **DBY** (Deutsche), **EBY** (Eurobank Tekfen), **HYA** (HSBC), **OSM** (Garanti), **TYS** (Türk Ekonomi). **This is exactly why no broker list exists** (what Matriks couldn't give): foreign-ness is a per-custody-ACCOUNT property MKK tracks, not a per-broker label. Textbook proof of the user's rule: **Garanti = GRM broker (domestic) + GPS portföy-saklama (domestic) + OSM YABANCI (foreign)** — three codes, one institution. Curated foreign-EXECUTION broker set (for broker-netting) also captured: MLB, MLM, HSY, MSI, JPM, GSI, CSM, DSI, UBS, UCI, CIM, ESI, REN, BNP, AHG, ACM, CMY (+ BAR Barclays, on Takasbank but dormant in Matriks). Domestic-despite-foreign-parent exclusions encoded: GRM, GNI(ING), FNY(QNB), TBY(TEB/BNP).
- **Q2 — answered.** `v1/investor/historic` still HTTP 400 across ALL dates/modes tested (2015–2025) via the foreignInvestorMode=historic path → server-side licensing/endpoint issue, NOT a param-shape bug. Chosen custody path doesn't need it. Stop chasing.
- **Q3 — answered, and it reshapes the design.** **Custody/takas history is DEEP — verified back to 2011-06-30** (CIY 59% / DBY 22% of GARAN custody even then). **Broker-level AKD flow is RECENT-ONLY** — 400 'veri bulunamadı' for 2015/2018/2020/2023/2024, data only ~2025+. So the two foreign-flow paths have very different depth — this is the "there might be limits" the rep meant.
**Decided:** **Foreign-flow factor PRIMARY = custody-based** (yabancı takas ratio / net change in the 6 YABANCI custodian positions; served by Matriks `historicalData includeHistoricalTakasIndicator`, already a golden). Custody-based by construction (dissolves the parent-nationality problem) and spans the full backtest window. **Broker-netting (AKD) = recent-period (~2025+) overlay/cross-check only**, not the multi-year leg. EVDS weekly non-resident = independent macro cross-check (unchanged). No ADR yet — this refines the foreign-flow leg within the existing design; promote to ADR if the live build confirms.
**Then built (same session, offline slice):** `src/tmkg/factors/foreign_custody.py` — a committed-reference loader (mirrors `bist_isin_adapter`) that reads/validates `foreign_custody_codes.json` and exposes `foreign_custody_codes()` / `foreign_execution_brokers()` / `domestic_exclusions()`. Validate-then-trust: schema-version check, code shape, non-empty custody set, and **bucket disjointness** (a code can't be both custody and execution/domestic — would double-count/mis-sign the §5 flow). Updated registry FFLOW: source `matriks`/series_id `takas_yabanci`, note now custody-based (DIFF on the takas ratio, ≥2011), **kept `status=BLOCKED`** — honestly still blocked, but on the *ingestion* not an unknown list. +8 tests (`test_foreign_custody.py`). **verify GREEN — 220 passed / 2 skipped.**
**Open:** FFLOW stays `BLOCKED` until the takas custody-series ingestion lands its L2 series. **Precise data finding (saves the next session a wrong turn):** neither existing golden carries the foreign/domestic split the *primary* custody factor needs — `takas_GARAN_2025_shock` is the **aggregate** `tV_series` only (no per-custodian split); `foreign_flow_GARAN_historic_2025Q1` is the **recent-only AKD broker** overlay. The per-custodian takas **time series** (sum the YABANCI codes → foreign held, daily) comes from `settlementAnalysis mode=symbol`, but the interactive MCP returns it as **formatted TEXT**; the REST pipeline (`MatriksAdapter`) gets a different **raw JSON** shape that has **not been observed yet**. So step 1 of the live session is a network probe to capture that raw settlementAnalysis payload as a new golden — then the parse + net-foreign machinery can be built/tested against it (guessing the shape offline = fabricating the payload contract, §4/§5; deliberately not done). The loader (`factors.foreign_custody`) is built+tested and ready to feed that ingestion.
**Next action:** LIVE ingestion session — source the Matriks-served factors (EURTRY, BRENT, gold, BIST sector indices) + VIX, then build the **custody-based foreign-flow ingestion** consuming `foreign_custody.foreign_custody_codes()` (net the YABANCI takas positions → L2 `FFLOW`, ≥2011), flip FFLOW→available with that ingestion's test, then run `run_m2_factor_model(require_all_factors=True)` over real 2024–2025 and check the M2 exit gate.

---

## 2026-06-21 — M2 exit-gate evaluator + factor mapping CONFIRMED (milestone M2, IN PROGRESS)
**Context:** continuation of the same offline session (user collecting the Matriks custodian list tomorrow). **User CONFIRMED the gold→energy, VIX+MSCI-EM→market rung mapping** flagged in the prior entry — no change needed; the registry stands as committed (`a34ec1c`). Then built the one remaining offline piece of the M2 exit gate.
**Did:** Built the **M2 exit-gate evaluator** `src/tmkg/factors/diagnostics.py` — the *judge*, before the live data lands (BUILD_PLAN sequencing rule 2). Two of the four M2 gate criteria were already guarded structurally (residual orthogonality invariant; "no factor silently dropped"); the other two were only *mechanism*-tested and lacked an evaluator that produces a verdict over a real fit:
1. **"factor model explains a plausible variance share per universe_class"** — `factor_variance_share` (per-name R² = 1 − var(residual)/var(ret) on overlapping dates; skips thin/zero-variance names, §4) + `variance_share_by_class` (median + IQR per class; a NULL class is surfaced as `(unclassified)`, never dropped).
2. **"betas break across the 2025-03-19 shock"** — `assess_regime_break`: per (symbol, factor) the across-boundary shift `|mean_after − mean_before|` measured against the **within-regime std (the noise floor)**; `break_ratio = shift/(within_std+eps)`. Aggregate = median break_ratio per factor (≫1 = genuine break, ≈1 = betas indistinguishable across the shock → red flag). Defaults to the `orthodox_turn_2023`→`imamoglu_shock_2025` regime pair.
3. **`m2_gate_diagnostics(store, as_of, …)`** — the only L2-touching fn; reads `total_returns`/`residuals`/`betas` back **through PITAccess** (§4) and emits the two measurements as a report-ready dict. The live session calls this after the fit.
**Result:** **verify GREEN — 213 passed / 2 skipped** (was 206/2; +7). New tests: `tests/factors/test_diagnostics.py` (5 — R²=1 for a fully-explained name / R²=0 for an unexplained one / definition match; thin+zero-variance skip; per-class aggregation incl. the unclassified group; a flipped-beta factor scores break_ratio≫20 while a stable factor stays <3; one-sided window emits no break) + `tests/l2/test_m2_gate_diagnostics.py` (2 — the reader plumbs L2→evaluators with exact known answers r2_median≈0.70 / shift≈3.0; and the **PIT gate hides post-shock betas** at an early as_of so no two-sided break is fabricated). The 2 skips remain M3 AST-scan + L1 provenance soft-edges.
**Decided:** the variance-share/regime-break **evaluator is built before the fit** so the live run emits an honest verdict (no eyeballing betas into significance). R² reported raw (a slightly-negative R² is a real "model fits this name poorly" signal, not clipped). Break is measured against the within-regime noise floor, not as an absolute Δ (a big beta that wobbles a lot has not "broken"). No ADR (implementation faithful to the M2 gate).
**Open / still blocked (unchanged):** foreign-flow leg (FFLOW) blocked on Matriks Q1; the rest of the unsourced core set + the real fits are network-gated for the live session. With this slice, **every offline-buildable M2 piece is done** — the machinery, the registry/manifest, and now the gate evaluator. What remains genuinely requires the network/custodian list.
**Next action (unchanged):** **Live ingestion session** — source the Matriks-served factors + VIX into L2 driven by `registry.specs()`; build FFLOW once Q1 lands; `run_m2_factor_model(require_all_factors=True, order=None)` over real 2024–2025; then `diagnostics.m2_gate_diagnostics(store, as_of=…)` to read the exit-gate verdict (variance share per class + the 2025-03-19 beta break).

## 2026-06-21 — M2 factor-set registry + ladder role/name fix (milestone M2, IN PROGRESS)
**Context:** user is collecting the pending Matriks answers (Q1 custodian list) tomorrow; asked to keep building whatever is **not** blocked on that. So this session is the offline, unblocked half of the M2 exit-gate prep: turn the live ingestion session from ad-hoc into a config-drive, and fix a latent bug the synthetic tests were masking.
**Did:**
1. **Canonical factor-set registry** `src/tmkg/factors/registry.py` — the design's core factor set (§ lines 92/193/291) encoded as data: each `Factor(name, role, method, source, series_id, status, note)`. `role` ∈ `neutralize.DEFAULT_LADDER` (the §200 rungs); `method` is `simple`/`diff` per the level type (yields/CDS/VIX → `diff`). Derivations the engine consumes: `specs()` ({name→method}), `ladder_order()` (factor **names** in §200 rung order), `order_present()` (orders an arbitrary *landed* name set by rung — robust to partial runs / vendor renames), `blocked_factors()`. `validate()` enforces well-formedness (known role/method/status, no dup, every rung represented). Factor set: market = XU100 + MSCIEM(EEM) + VIX; fx = USDTRY + EURTRY; rates_cds = TRY2Y/TRY10Y/TRCDS5Y (diff); energy = BRENT/NATGAS/GOLD; sector = XBANK/XUSIN/XKMYA/XELKT/XGIDA/XUTEK (principal subset); foreign_flow = FFLOW (**status=blocked** on Matriks Q1); holding = XHOLD (proxy for the M2-derived holding-group factor).
2. **Fixed the role/name ladder mismatch** in `ingest/pipeline.py`. `build_residuals`/`run_m2_factor_model` defaulted `order` to a tuple of ladder **roles** (`("market","fx",…)`), but `build_residuals` intersects `order` against the panel's factor **names** (XU100, USDTRY…). With real names that intersection is **empty → nothing stripped → a fake "residual" equal to the raw return** — the most dangerous M2 bug (§5). It only passed because the synthetic tests named their factors after the roles. Now `order=None` (new default) derives the concrete name order from `registry.order_present(present)`; an explicit `order` is still honored verbatim (existing tests unchanged). Removed the role-keyed `_DEFAULT_LADDER_ORDER`.
3. **Hardened `scripts/verify.sh`** to prefer `.venv/bin/python` (the BUILD_LOG ③ minor item) — no more bare `python` resolving to a dep-less interpreter.
**Result:** **verify GREEN — 206 passed / 2 skipped** (was 193/5; +10 new registry/ladder tests, +3 live Matriks tests ran with `.env` sourced). New tests: `tests/factors/test_registry.py` (8 — well-formedness, specs excludes blocked FFLOW, ladder rung-order over real names, `order_present` scramble + unknown-name-after-known-rungs) and `tests/l2/test_m2_real_factor_names.py` (2 — a residual run with **real** names XU100/USDTRY through L2 yields `strip_order="XU100>USDTRY"` and a pure-factor name neutralizes to ~0; the regression that would catch the bug above). The 2 skips remain M3 AST-scan + L1 provenance soft-edges.
**Decided:** strip order is **concrete factor names in §200 rung order, derived from the registry** — never retyped, never role-keyed. The registry is the single manifest the live ingestion session drives from (`specs()` feeds `build_factor_return_panel`, `ladder_order()`/`order_present()` feed the strip). No ADR (implementation faithful to §200/§5).
**⚠️ CONFIRM with user (design-semantics, flagged not silently deviated per §1):** §200's ladder names 7 rungs but the factor set (§92) has 3 extras the ladder doesn't explicitly place — **gold, VIX, MSCI-EM**. I mapped **gold → energy/commodity** rung (the §200 rung is literally "energy/commodity"; gold is a commodity), and **VIX + MSCI-EM → market** rung (global risk-on/off, stripped first with XU100). Defensible and a one-line registry edit if you disagree — but it touches the residual claim, so please confirm before the live fit.
**Open / still blocked (unchanged):** the foreign-flow leg (FFLOW) stays `status=blocked` until Matriks Q1 (custodian-code list) returns — its ladder slot is reserved so it drops in with no rework. Everything else in "core factor set still unsourced" is **network-gated**, for the live session.
**Next action (unchanged from prior entry):** **Live ingestion session** — source the Matriks-served factors + VIX into L2 driven by `registry.specs()`, build FFLOW once Q1 lands, then `run_m2_factor_model(require_all_factors=True, order=None)` over real 2024–2025 and check the exit gate (regime break at 2025-03-19; variance share per `universe_class`). First confirm the gold/VIX/MSCI-EM rung mapping above.

---

## 2026-06-20/21 — M2 compute machinery built end-to-end (milestone M2, IN PROGRESS)
**Did:** Built the entire **pure/offline M2 factor-model machinery** in 5 green vertical slices (commits `248971f`, `8dc4f26`, `1b76c9b`, `4ccc066`, `1c7cfae`):
1. **Factor-return constructor** `factors/series.py::compute_factor_returns` — turns M1's L2 factor *levels* into returns; per-factor rule `simple`/`log` (multiplicative price-FX-index-commodity) vs `diff` (additive rate levels: yields/CDS/VIX); §4 no-fabrication (gaps not filled, first-obs NaN, single-obs all-NaN, `ln(0)` refused). 9 unit + 2 golden tests (USDTRY/XU100 day-by-day).
2. **Market-regime labeler** `factors/regime.py` — BIST structural breaks (2018, 2021-22, 2023 orthodox turn, **19-Mar-2025 İmamoğlu shock**, Aug-2025 short-ban lift); injectable. Distinct from `returns.accounting_regime`.
3. **Rolling regime-aware beta engine** `factors/betas.py` — multiple-regression (partial) betas via Ledoit–Wolf-shrunk factor covariance (§201 p≈n); windows confined to the end date's regime → **no straddle → betas break across a boundary by construction** (§65, exit gate); §4 (NaN-drop, thin-window refusal, `knowledge_date=window-end`). 13 tests incl. the regime-break/no-straddle guarantee.
4. **Neutralization ladder** `factors/neutralize.py` — strips factors in the explicit order (§200 market→fx→rates/cds→energy→sector→foreign_flow→holding); residual is the stable least-squares projection (exactly orthogonal to every stripped factor, robust to collinear panel), ladder order drives per-rung variance attribution via Gram–Schmidt. New **§5 invariant** `tests/invariants/test_neutralization_orthogonality.py` (orthogonal to each rung incl. foreign_flow; teeth-check that an un-stripped factor stays correlated).
5. **Compute pipeline** in `ingest/pipeline.py` (`build_factor_return_panel`/`build_betas`/`build_residuals`/`run_m2_factor_model`) — PIT reads + L2 writes (the `build_total_returns` precedent), tags rows with PIT-known `universe_class`, lands L2 `betas`/`residuals`, §4 audit report. Plus the **"no factor silently dropped"** guard (`factor_coverage`, `require_all`) — exit-gate criterion. End-to-end L2 test recovers known betas to machine precision through a real DuckDB round-trip; PIT gate holds on built betas.
**Result:** **`make verify` GREEN — 186 passed / 2 skipped** (invariant suite 20, golden 16). All new code is pure or ingestion-layer; the no-network invariant still holds (factor modules import no network client).
**Decided:** (a) Factor returns computed **on read from PIT-fed levels**, NOT persisted into append-only `factors.ret` (the M1 `ON CONFLICT DO NOTHING` land can't backfill anyway). (b) Neutralization uses the **unshrunk OLS projection** (orthogonality must be exact); Ledoit–Wolf shrinkage is for the *beta* estimate and the M3 residual-covariance, not the strip. Neither is ADR-worthy yet.
**Foreign-flow quirk RESOLVED (2026-06-21, commit `96d37ff`).** Live MCP probes with the new MKK license: the quirk was a *mode* issue — `mode='monthly'` is a current-snapshot service; **`foreignInvestorMode='historic'` + `startDate`/`endDate`** returns broker-level history with foreign houses classifiable (BofA=MLB, HSBC=HSY). **Takas custody** (`historicalData includeHistoricalTakasIndicator`) is also live & dated (and shows the 3/19 shock as −23% custody value). Both captured as value-anchored goldens (`foreign_flow_GARAN_historic_2025Q1.json`, `takas_GARAN_2025_shock.json`) with 2 pinning tests. **Decisions (user):** foreign-flow = **broker-netting + EVDS weekly non-resident cross-check, DAILY** granularity. **Still 400 (escalate to vendor):** `v1/investor/historic` (%-non-resident demographic series) — but the chosen path doesn't need it. **VIX confirmed NOT in Matriks** (`symbolSearch`→0) → FRED needed.
**FRED adapter DONE (2026-06-21, commit `ec08679`).** `FRED_API_KEY` received & verified live (in gitignored `.env`; `config.FRED_API_KEY`). `tmkg/ingest/fred.py::FredAdapter` (legacy api_key **query-param** API — the user's Bearer note is FRED's v2/bulk API; ours is incremental so legacy is the right call, same key works for both, decision documented in-module). VIX/VIXCLS lands in L2 `factors` via `ingest_fred_series`; value-anchored golden `tests/golden/fred/vixcls_2025-03.json`; smoke_check passes live (16 prints match). 7 offline + 2 live tests.

**⚠️ PENDING — awaiting Matriks customer relations (user is calling them; plain-Turkish ask delivered 2026-06-21). Do NOT mark M2 factor-set complete until these are back:**
- **Q1 (the blocker):** the official **foreign-custodian (yabancı saklamacı) aracı-kurum code list** — which broker codes count as foreign/non-resident *custody*. Needed to build the foreign-flow broker-netting map; only MLB (BofA) + HSY (HSBC) verified so far. Classification is custody-based, NOT parent nationality (GARANTI BBVA = domestic despite BBVA parent).
- **Q2:** is `v1/investor/historic` (per-investor %-non-resident series, currently HTTP 400 even with MKK) in our license? higher VAP tier? or a param-shape issue (`investorDates` vs range)? — *not* a blocker (chosen path doesn't use it), but resolve.
- **Q3:** how far back broker-level `historic` foreign flow + takas custody history goes (backtest-window depth, ideally ≤2015).

**Open / exit-gate remaining (NETWORK-GATED — next is a live session):** ① **core factor set** still mostly unsourced — have market/FX (L2) + VIX (FRED adapter ready); still need EUR/TRY, Brent/gas, gold, BIST sector indices (all Matriks `ingest_factor_series`), **foreign-flow** (broker-netting — blocked on Matriks Q1), rates/CDS proxy (W3 scrape), holding-group, MSCI-EM/EEM (try Matriks `foreignMarkets` first). ② "betas break across 2025 shock" + "plausible variance share per class" need a **real fit over 2024–2025 data**. ③ Minor: `make verify` calls bare `python`; run under `.venv` (Python 3.13) or harden `scripts/verify.sh`.
**Next action:** **Live ingestion session** — source the Matriks-served factors (EURTRY, BRENT, gold, sector indices via `ingest_factor_series`) + VIX (`ingest_fred_series`) into L2; build the foreign-flow factor once Matriks returns the custodian list (Q1); then run `run_m2_factor_model(require_all_factors=True)` over real 2024–2025 and check the exit gate (regime break at 2025-03-19, variance share per `universe_class`).

---

## 2026-06-20 — M1 CLOSED ✅ (milestone M1 complete)
**Did:** Formally closed M1. Wrote the **exit-gate checklist into `BUILD_PLAN.md`** (M1 header now `✅ COMPLETE (2026-06-20)` with a criterion→evidence table mirroring the M0 format), and **materialized the one missing §4 artifact**: ran `run_m1_ingestion` **live** end-to-end (EREGL daily bars + USDTRY + CPI_TUFE, as_of 2024-12-31) against the real Matriks + EVDS APIs, into a throwaway L2 store, writing the genuine `data/cache/m1_ingestion_report.json` (47 bars, 46 total returns, `ret_usd_null=0`). This proves the full orchestrator runs live, not just under the stubbed unit test. Explicitly recorded the two remaining threads as **deferred / non-blocking** (not gate criteria), carried into M2.
**Result:** **`make verify` GREEN — 146 passed / 2 skipped**; `make smoke` PASS. **M1 exit gate met on every criterion** (see the BUILD_PLAN table): total-return corporate-action reconciliation (EREGL/ASELS rights), USD-primary + CPI-real-TRY end-to-end through L2/PIT (FY2023 EREGL real −30.35%), limit-lock censoring, staleness flags, real delisted name in the as-of universe (SODA), accounting_regime across both FY2023/FY2025 switches with real declaration-dated rows, reconciliation reports on disk. The 2 skips are genuinely later milestones (M3 signal-AST-scan; L1 provenance soft-edges).
**Decided:** M1 is **DONE**. The live orchestrator run's `ret_real_try_null=46` (all NaN over a daily window) is **correct, not a bug** — CPI is monthly and real-TRY is a monthly cross-check; daily bars only get a real return on the rare month-start overlap (documented in BUILD_PLAN's deferred note + slice 6). Two threads deferred to M2 with rationale: (a) a real declared-dividend full-TR reconciliation (the yield mechanism is fixture-tested + correct; an end-to-end real-dividend check additionally needs the vendor **unadjusted** ex-date close, a fundamentals-data task that fits the M2 pull) — NOT a gate criterion (the bedelsiz/rights reconciliation satisfies it); (b) SODA's true IPO `valid_from` (documented sourced lower bound; true IPO unsourceable for the delisted entity).
**Open:**
- The two deferred threads above — pick up (a) alongside the M2 fundamentals pull.
- M2 has no scaffolding yet — the factor tables (`factors`, `betas`, `residuals`) exist in schema but no factor ingestion/fit code.
**Next action:** Begin **M2 — factor model + neutralization** (BUILD_PLAN.md). First slice: land the **foreign-flow factor** into L2 — it leads because §5 calls it out as THE BIST comovement driver (if it's not stripped, flow-driven comovement masquerades as residual linkage and fabricates supply-chain signals). Source = Matriks `institutionalFlow` (note the M0 caveat: it ignored `foreignPeriods` and returned the latest month — verify the historic/period pull or filter client-side). Then the rest of the core factor set (market, FX, rates/CDS proxy, energy, sector indices) and rolling Ledoit–Wolf betas per `universe_class`.

## 2026-06-20 — M1 slice 7: accounting_regime fundamentals ingestion (the missing consumer) (milestone M1, in progress)
**Did:** Built the consumer the accounting_regime state machine was missing — nothing wrote regime-tagged rows into the L2 `accounting_regime` table from real fundamentals. New `pipeline.ingest_accounting_regime(adapter, store, symbol)`: pulls the vendor's declaration-date history (`fundamentalAnalysis(includeDeclarationDates=True)`), maps each period via `regime_for_period` (nominal_pre2023 / ias29_2023_2024 / suspended_2025_2027), and lands rows with **`knowledge_date = declarationDate`** so a period's regime tag inherits the same PIT gate as the fundamental it describes. New `MatriksAdapter.parse_declaration_periods`. **Live reshape finding (cost the first red):** the committed `declaration_dates_KCHOL.json` golden was the M0 *curated/flattened* shape (`data.periods = [{period, declarationDate}]`); the **real** live contract is `payload['declarationDates']['items'][*]['periods'][*]` — and top-level `payload['periods']` is just recent statement-period *codes* (a trap). Per §4 (parse what the API really returns) I **re-captured the golden raw** from live (67 periods back to 200812, the full immutable declaration history) and parse the real shape. Updated the two other consumers of the old shape to go through the production parser (`test_golden_integrity`, `test_pit_leak` — so the PIT-leak detector now exercises shipped code, not a parallel copy). Tests: `tests/l2/test_accounting_regime_ingestion.py` (3, offline through L2+PIT — regime mapping across both switches, knowledge_date=declarationDate, and the declaration-date PIT gate worked example: as_of 2025-04-15 → latest known 202412, 202503 invisible) + 1 **live** drift test in `test_matriks_live.py` reconciling the same KCHOL gate over the real network.
**Result:** **`make verify` GREEN — 146 passed / 2 skipped** (was 142/2; +4: 3 offline + 1 live, all ran — Matriks up). `make smoke` PASS (both sources). The 2 remaining skips are the correct future-milestone invariants: `test_no_network_in_signal_layer` (M3 AST-scan, no signal modules yet) and `test_provenance` (soft edges to L1, not yet written). The slice-5/6 "accounting_regime tagged but no consumer" open thread is **CLOSED** — real declaration-dated regime rows now land in L2 and obey the PIT gate.
**Decided:** The regime tag is gated by the declaration date (`knowledge_date = declarationDate`), not the period-end — a period's regime is only "known" once the fundamental is declared, same posture as the fundamental itself. Declaration-dates golden re-captured to the **raw live shape** (`declarationDates.items[*].periods`) — supersedes the M0 curated capture; declaration dates are immutable history so it's a value anchor, not just reachability. No ADR (parser/ingestion implementation, consistent with §4 and the M0 "re-capture raw single-call goldens" follow-up).
**Open:**
- The two remaining M1 skips are genuinely later milestones (M3 signal-layer AST scan; provenance soft edges to L1). Not M1 blockers.
- Fundamentals **VALUES** (the adjusted-vs-unadjusted figures in `accounting_regime_KCHOL_202412.json`) are still not landed in any L2 table — only the regime *state* per (symbol, period) is. Storing the actual two-basis figures is an M2 fundamentals concern (factor/intensity inputs), not needed for the §5 regime invariant.
- Still nice-to-have before declaring M1 fully done: a real declared-dividend full-TR reconciliation through the pipeline; a real BIST limit-locked sequence as a value anchor; SODA's true IPO `valid_from` backfill (slice 6 lower-bound caveat).
**Next action:** Take stock against the **M1 exit gate** (BUILD_PLAN.md M1): total-return golden ✓, limit-lock ✓, delisted name ✓, accounting_regime across both switches ✓, reconciliation reports ✓ — the core gate is met. Either (a) close M1 formally (write the exit-gate checklist into BUILD_PLAN + a real declared-dividend reconciliation to fully retire that thread), or (b) begin **M2 — factor model + neutralization** (core factor set into L2; the foreign-flow factor first, since it's the §5 comovement driver). Recommend confirming M1-close with the user, then starting M2.

## 2026-06-20 — M1 slice 6: CPI wired into the pipeline end-to-end + REAL delisted name (SODA) (milestone M1, in progress)
**Did:** Closed two M1 gaps. **(1) CPI into the pipeline (end-to-end).** Slice 5 left `ret_real_try` computable in the *pure* `compute_total_returns(cpi=...)` but **never ingested** — the pipeline never fed it, so it landed NULL in L2. Now wired: new `pipeline.ingest_cpi(EvdsAdapter, store, …)` (fetch→`parse_cpi`→`write_parquet('factors')`); `build_total_returns` now **reads CPI back through `PITAccess`** (factor-filtered, alongside FX) and passes it to the constructor, so `ret_real_try` actually persists to `total_returns`; `run_m1_ingestion` gained optional `cpi_adapter`/`cpi_window` (no break to existing callers — the daily EREGL anchor test still lands `ret_real_try` NULL since no CPI is in scope there). New `tests/l2/test_cpi_pipeline_end_to_end.py` (3): CPI lands in `factors` with the right release `knowledge_date` (Jan→2023-02-03, Dec→2024-01-03); the full EVDS→L2→PIT→construct→L2 round-trip reconciles **FY2023 EREGL real −30.3486% / nominal +7.6116%** read back from L2; and **the CPI knowledge-date gate bites** — a build dated 2023-12-15 leaves December's `ret_real_try` NULL (Dec CPI prints 2024-01-03) while keeping nominal + November's real return (no future print borrowed, §4). **(2) Real delisted name (the W2 wall on real data).** Sourced **SODA (Soda Sanayii A.Ş.)**, delisted 2020-09-30 absorbing into Şişecam (SISE). Authoritative dates (Borsa İstanbul announcement via QNB Invest, corroborated Erdem&Erdem / Şişecam IR): last trading day 2020-09-30, delisted 2020-10-01, delisting **announced ~2020-09-18**, merger announced 2020-01-30. Committed provenance golden `tests/golden/borsa/delisting_SODA_2020.json`; new `ingest/survivorship.py::ingest_delisting`; modelled **bitemporally as two rows** (open membership known 2020-01-30; delisting correction known 2020-09-18). **Enhanced `PITAccess.universe()`** to resolve the latest correction known by `as_of` per (symbol, universe, valid_from) span (window-function `ROW_NUMBER`), so a read before the delisting was announced honestly shows SODA still-open (no look-ahead) — the synthetic `DLSTD` test couldn't show this. New `tests/invariants/test_real_delisting.py` (3); the existing synthetic survivorship test stays green (the change is backward-compatible for single-row spans).
**Result:** **`make verify` GREEN — 142 passed / 2 skipped** (was 136/2; +6: 3 CPI-pipeline + 3 real-delisting; the 2 live Matriks tests also ran/passed, network up). The 2 skips remain the correct future-milestone invariants (accounting_regime M1-fundamentals, no-network AST scan M3). `ret_real_try` now demonstrably persists through the real DuckDB+PIT round-trip — slice 5's "computed but not ingested" gap is **CLOSED**.
**Decided:** CPI is read back through PIT like any factor (FX and CPI co-live in `factors`, selected by name) — a month's `ret_real_try` only lands once that month's CPI print was knowable at `as_of`, else NULL (never an interpolated deflator). Delistings are modelled as a **bitemporal two-row correction** and `universe()` applies the latest-known version per span — no ADR (implementation of the §5 survivorship + PIT invariants). **Honest sourcing caveat (§4):** SODA's true IPO/first-listing date could **not** be cleanly sourced (vendors surface the unrelated SODSN; KAP/Şişecam PDFs not machine-readable), so `valid_from=2020-01-30` is a documented **sourced lower bound** (merger-announcement date) — every test assertion sits inside the airtight 2020-06..2020-10 sourced window; nothing depends on the bound.
**Open:**
- **SODA's true first-listing date is a refinement** — backfill `valid_from` when an authoritative IPO date is sourced (KAP company 1072 "İlk İşlem Tarihi" tab wasn't machine-readable via WebFetch; an interactive Matriks/KAP pull may get it).
- CPI real-TRY in the pipeline is exercised on the **monthly** EREGL golden (CPI is monthly, so daily prices only get a real return on the rare month-start overlap — by design it's a monthly cross-check, not a daily series). A daily name's `ret_real_try` is mostly NULL by construction; that's correct, not a bug.
- Still owed before the M1 exit gate: **accounting_regime** fundamentals ingestion (the consumer that writes regime-tagged rows from Matriks `fundamentalAnalysis`; unskip `test_accounting_regime`); full-dividend total-return reconciliation against a real declared dividend through the pipeline; a real BIST limit-locked sequence as a value anchor.
**Next action:** Build the **accounting_regime fundamentals ingestion** — a Matriks `fundamentalAnalysis` adapter that writes regime-tagged rows to the L2 `accounting_regime` table (nominal_pre2023 / ias29_2023_2024 / suspended_2025_2027), then unskip `tests/invariants/test_accounting_regime.py`. That's the last network-bearing M1 invariant without a consumer.

## 2026-06-20 — M1 slice 5: EVDS (evds3) CPI adapter + CPI-real-TRY reconciliation (milestone M1, in progress)
**Did:** Built the CPI-real-TRY cross-check the M1 exit gate calls for. **First resolved the evds3 REST contract** (the standing blocker — prior memory: "contract not determined, blind probing declined"). The legacy `evds2.tcmb.gov.tr/service/evds/...` path is DEAD: it unconditionally 302-redirects (UA-independent) to the evds3 React SPA, and `evds3/service/evds/...` is caught by the SPA catch-all (HTTP 200 + HTML, not data) — confirmed live. Found the real API by reading the SPA's `index-*.js` bundle (`axios baseURL="/igmevdsms-dis"`) and cross-checking the `evdspy` pip package source. **Resolved contract (live-verified):** `GET https://evds3.tcmb.gov.tr/igmevdsms-dis/series=<CODE>&startDate=DD-MM-YYYY&endDate=DD-MM-YYYY&type=json`, auth `key:<EVDS_API_KEY>` **header** (post-2024 change), response `{totalCount, items:[{Tarih:"2023-1", TP_FG_J0:"1203.48...", UNIXTIME}]}` (value column = series code dots→underscores). Then built `src/tmkg/ingest/evds.py` `EvdsAdapter` (fetch / parse_cpi→`factors`-schema rows / smoke_check), mirroring `MatriksAdapter`: fail-loud `SourceUnreachable` on missing key / transport / HTTP error / **the SPA-HTML-at-200 trap** (a moved endpoint serving index.html is treated as unreachable, never parsed as data, §4); `ContractDrift` on a JSON envelope without `items`. Headline series **`TP.FG.J0`** (TÜFE genel, 2003=100, monthly), factor `CPI_TUFE`. `parse_cpi` maps `Tarih` `YYYY-M`→`bar_date` (month-start), value→float (drops non-numeric/`ND`, raises if all drop), `knowledge_date` = 3rd of the next month (TÜİK release convention, PIT-honest). Captured two REAL goldens: `tests/golden/evds/cpi_TP.FG.J0_2023.json` (immutable VALUE anchor) + `tests/golden/matriks/ohlcv_EREGL_monthly_2023.json`. Fed CPI into the existing `compute_total_returns(cpi=...)` and reconciled.
**Result:** **`make verify` GREEN — 136 passed / 2 skipped** (was 123/2; +13). `make smoke` PASS (now proves BOTH sources — added `check_evds`). New: 4 golden CPI-reconciliation tests (`tests/golden/test_cpi_real_try_reconciliation.py`) + 9 EVDS adapter tests (`tests/test_evds_live.py`, incl. 1 live drift guard that ran — EVDS up — and skips cleanly if down). **Hand-verified real anchors now frozen:** 2023-02 nominal +15.7480% / real +12.2187% (CPI +3.1450%); 2023-08 nominal +3.1654% but real −5.4281% (inflation overtakes a nominal gain); **FY2023 EREGL nominal +7.6116% vs real −30.3486%** (CPI +54.5003%) — the vivid proof of why USD is primary and nominal TRY is reference-only (§3). Plus a parameter-free identity anchor: a CPI-tracking asset (price≡CPI) has ~0 real return every month.
**Decided:** evds3 programmatic API = `/igmevdsms-dis` gateway, `key` header auth (recorded in the [[evds-migration-evds3]] memory, which superseded its "contract not determined" state). No ADR — it's an external-contract finding, consistent with §4 (parallels the Matriks ADR-0002 transport finding but EVDS is a secondary cross-check source, not the spine). CPI lands in the L2 `factors` table (factor=`CPI_TUFE`), not a new table. The fixtures-never-reach-L2 guard required **no tuple change**: `evds.py` is under `ingest/`, already in `_L2_INGRESS_PACKAGES`, so it's auto-covered (verified — the guard stays green).
**Open:**
- **CPI `knowledge_date` is the 3rd-of-next-month convention, not the exact TÜİK release date.** PIT-honest (never early) but approximate; refine from the TÜİK release calendar when bitemporal CPI reads matter (M2 factor model). `bar_date` = month-start.
- **CPI ingestion is not yet wired into the L2 pipeline.** `parse_cpi` yields `factors`-schema rows and the reconciliation runs through the pure constructor, but no `ingest_cpi`→`L2Store.write_parquet('factors', ...)`→PIT-read path exists yet (the M1 pipeline lands prices/FX/total_returns; CPI is the next pipeline addition, alongside writing `ret_real_try` into `total_returns` end-to-end).
- Remaining M1 debts unchanged: ingest one **real delisted** BIST name end-to-end (survivorship); full-dividend total-return reconciliation against a real declared dividend through the pipeline; a real BIST limit-locked sequence to upgrade limit-lock to a value anchor.
**Next action:** Wire CPI into the M1 pipeline — add `ingest_cpi` (EvdsAdapter.fetch→parse_cpi→`L2Store.write_parquet('factors')`, bitemporal) and have `build_total_returns` read CPI back through `PITAccess` and populate `ret_real_try` end-to-end (so the real-TRY column is landed in L2, not only computed in the pure constructor). Then the real delisted-name end-to-end for survivorship.

## 2026-06-20 — M1 slice 4: unskip fixtures-never-reach-L2 guard (milestone M1, in progress)
**Did:** Closed the cheap §4 win the slice-3 next-action queued. Implemented and unskipped `test_fixtures_never_reach_l2` in `tests/invariants/test_no_fabrication.py`. The honest enforcement isn't behavioral (L2Store has no provenance guard to trip) but **structural**: an AST source-scan of the **L2-ingress packages** (`tmkg.l2`, `tmkg.ingest`, `tmkg.returns` — the only code that can land rows in L2) asserting none of them reference `fixtures/` (string constant containing "fixtures", or any use of `config.FIXTURES_PATH`). Comments are excluded (AST-only). Matches the no-network test's "scan sources" idiom (M3). **Key scoping finding:** `fixtures/` IS legitimately read in production — but only by the **v1 `tmkg.loaders`** package (`identity.py`, `ownership.py`), which writes the **L1 Kuzu identity graph**, never L2. So the invariant is correctly L2-scoped, not "no src touches fixtures" (which would false-fail on the L1 loaders). Verified teeth with a negative control: planting `"...fixtures/companies.json"` into `returns/staleness.py` makes the test fail and names the offending file; restored clean.
**Result:** **`make verify` GREEN — 123 passed / 2 skipped** (was 122/3 at session start; the unskipped test now passes). Invariant suite 14 passed / 2 skipped. The 2 remaining skips are the correct future-milestone invariants: `test_no_network_in_signal_layer` (M3, AST-scan signal imports) and `test_provenance` (soft edges via L1 loaders). `make smoke` not re-run this slice (no network code touched; offline structural change only).
**Decided:** "Fixtures never reach L2" is enforced as an L2-ingress source-scan, not a runtime guard — fixtures feeding the L1 identity graph via `tmkg.loaders` is explicitly out of scope (different store, §4 is about the quant/market-data L2). No ADR (test/implementation choice, consistent with §4).
**Open:** Unchanged M1 debts toward the exit gate: **CPI-real-TRY via EVDS (evds3)** — `cpi` arg already exists in `compute_total_returns`, needs an EVDS adapter (TUFE/CPI series) + smoke_check + golden, then reconcile real-TRY on a known window; ingest one **real delisted** BIST name end-to-end (survivorship); full-dividend total-return reconciliation against a real declared dividend through the pipeline; a real BIST limit-locked sequence to upgrade limit-lock from fixture to value anchor. As future L2-ingress packages are added (e.g. an EVDS adapter, factors), they fall automatically under this guard's `_L2_INGRESS_PACKAGES` — extend that tuple when the EVDS adapter lands.
**Next action:** Start **CPI-real-TRY via EVDS (evds3)** — wire an EVDS adapter (TUFE/CPI series) with a `smoke_check()` drift guard + a committed golden, feed CPI into `compute_total_returns` (the `cpi` arg is already there), and reconcile real-TRY on a known window. Add the new adapter's package to `_L2_INGRESS_PACKAGES` in `test_no_fabrication.py` so it stays under the fixtures-guard.

## 2026-06-20 — M1 slice 3: limit-lock censoring + staleness + accounting_regime state machine (milestone M1, in progress)
**Did:** Built the three network-free M1 invariants toward the exit gate. (1) **Limit-lock censoring** (`tmkg/returns/limit_lock.py`): `flag_limit_lock` marks bars pinned at the ±band daily limit (`|close_t/close_{t-1}-1|` within tol of band; default ±10%, per-name band arg for widened/market-maker names); `censor_lock_windows` collapses each maximal lock run into ONE cumulative cross-window return on the resolving day (the run's censored daily returns → NaN), preserving the compounded series return — wired into `compute_total_returns` (no-op when nothing is locked, so the back-adjustment goldens are unchanged). (2) **Staleness** (`tmkg/returns/staleness.py`): `flag_staleness` marks no-trade (quantity 0/NULL → carried-forward) bars `is_stale` for M2's Dimson/Scholes-Williams screen; keyed on quantity not volume (FX has zero turnover by construction). Both detection passes now run in `pipeline.ingest_prices` so landed `prices` carry the flags (+ audit counts). (3) **accounting_regime** (`tmkg/returns/accounting_regime.py` + new `RegimeStraddle` error): the state machine `regime_for_period` (nominal_pre2023 / ias29_2023_2024 / suspended_2025_2027) and `guarded_growth`, which **refuses** a growth/intensity/materiality calc straddling the FY2023 or FY2025 switch unless the caller asserts `basis_converted=True`. Unskipped `test_refuses_cross_regime_growth`; deduped the local regime helper in `test_pit_leak.py` to import the canonical function.
**Result:** **`make verify` GREEN — 120–122 passed / 3–5 skipped** (the spread is the 2 live Matriks tests, which run when reachable and skip cleanly otherwise; this run: 122 passed / 3 skipped). Invariant suite 13 passed / 3 skipped (was 10/4 — accounting_regime no-straddle now live, new limit-lock invariant added). New tests: `tests/invariants/test_limit_lock.py` (flag+collapse rule), `tests/invariants/test_accounting_regime.py` (state machine + no-straddle, both switches, common-basis escape), `tests/returns/test_limit_lock.py` (7: detection, down-limit, cumulative replacement, compounded-return preservation, unresolved-run-stays-NaN, no-op), `tests/returns/test_staleness.py` (4). The 3 remaining skips are correctly future-milestone (fixtures-never-reach-L2, no-network AST scan M3, provenance soft-edges).
**Decided:** Limit-lock detection is on the back-adjusted close ratio (multiplicative adjustment preserves the ratio on normal days); a bar that is BOTH a corp-action ex-date AND a limit day is a rare edge deferred to M2. Censoring relocates the realized return to the lock-resolution day and NaNs the censored days — the compounded series return is invariant (tested). Cross-regime growth requires an explicit, auditable `basis_converted=True` — never a silent default.
**Open:**
- **accounting_regime is tagged but not yet consumed end-to-end**: the state machine + guard exist and the `accounting_regime` L2 table is PIT-tested, but no fundamentals INGESTION yet writes regime-tagged rows from Matriks `fundamentalAnalysis` (the consumer). That's the next fundamentals slice.
- `test_no_fabrication`'s fixtures-never-reach-L2 skip says "unskip once L2 loaders exist" — the pipeline loaders now exist, so this is **newly unblockable**; unskip it next (cheap §4 win).
- Still owed before the M1 exit gate: **CPI-real-TRY via EVDS (evds3)** (memory: evds3 migration done, key saved, no consumer yet); ingest one **real delisted** BIST name end-to-end; full-dividend total-return reconciliation on a real declared dividend through the pipeline. Limit-lock currently tested on controlled fixtures — a real BIST limit-locked sequence (e.g. a 2025-March-shock name) would upgrade it to a value anchor.
**Next action:** Unskip `test_no_fabrication` fixtures→L2 guard (loaders now exist), then start the **CPI-real-TRY** cross-check via EVDS evds3 — wire an EVDS adapter (TUFE/CPI series) with a smoke_check + golden, feed CPI into `compute_total_returns` (the `cpi` arg already exists), and reconcile real-TRY on a known window. Then a real delisted-name end-to-end for survivorship.

## 2026-06-20 — M1 slice 2: end-to-end ingestion pipeline + live reconciliation (milestone M1, in progress)
**Did:** Matriks recovered (yesterday's full 504 → today all tools serve; `make smoke` PASS again). Wired the end-to-end M1 ingestion the slice-1 next action called for. New `tmkg/ingest/pipeline.py`: `ingest_prices` / `ingest_factor_series` (fetch→`parse_bars`→`L2Store.write_parquet` for `prices` and `factors`), `build_total_returns` (reads the just-landed inputs back **through `PITAccess`** at an `as_of`, runs the pure `compute_total_returns`, lands `total_returns`), and `run_m1_ingestion` (orchestrates + writes the `data/cache/m1_ingestion_report.json` audit, §4). **Key live finding (cost me the first red):** `historicalData` returns *downsampled* `sampledBars` unless **`rawBars=True`** is passed, and caps at `limit` — so the pipeline always demands raw, complete bars with `limit = calendar-days-in-window` (a sampled series would be a silently-fabricated return path, §4). Tests: `tests/l2/test_m1_ingestion_pipeline.py` (4 offline, golden-backed adapter): anchor reconciles through the real DuckDB+PIT round-trip; the **built `total_returns` obey the PIT gate** (11-27 row invisible at as_of 11-26, both read-side and input-side); orchestrator audit shape. Plus 2 **live** tests in `test_matriks_live.py`: the M1 pipeline reconciles the EREGL anchor over the **real network**, and I fixed `_adapter_or_skip` to probe a real cheap tool (`symbolSearch`) instead of `openapi.json` — the latter was observed to time out *while tools serve fine*, so the live guard was skipping falsely.
**Result:** **`make verify` GREEN — 108 passed / 4 skipped** (was 102/5); `make smoke` PASS. The live tests now actually execute under verify (config auto-loads `.env`; Matriks up) and PASS — they auto-skip cleanly if Matriks is unreachable, so verify stays green on an outage. **Slice-1's "live parse-roundtrip not yet confirmed" open thread is now CLOSED** — fetch→parse→L2→PIT→construct→read reconciles the EREGL 2024-11-27 anchor live (TRY −1.3972% / USD −1.3803%). The 4 remaining skips are the correct future-milestone invariants (accounting_regime M1, no-network AST scan M3, provenance soft-edges, fixtures-never-reach-L2).
**Decided:** Ingestion MUST pass `rawBars=True` + a window-covering `limit` to `historicalData` — the default sampled response is not the true daily series (no ADR; it's an implementation contract, consistent with §4). Live tests probe a real tool for reachability, never `openapi.json`.
**Open:**
- Still owed before the M1 exit gate (unchanged from slice 1): **limit-lock** ±10% censoring + cumulative-across-lock-window; **staleness** flags; **accounting_regime** tag (unskip `test_accounting_regime`); **CPI-real-TRY** via EVDS (evds3); ingest one **real delisted** BIST name end-to-end. These three (limit-lock, staleness, accounting_regime) are network-free — buildable next regardless of Matriks state.
- Full-dividend total-return reconciliation against a real EREGL/ASELS dividend ex-date is still pending (the dividend-yield path is unit-tested on a fixture; not yet end-to-end on a real declared dividend through the pipeline).
- `marketPrice` requires an `action` param (`"price"`/`"inflation"`) — not used by M1 (we use `historicalData`); noted so a future caller doesn't trip on it.
**Next action:** Build **limit-lock censoring** (network-free): detect ±10% band-limit days (widened-band aware) on the `prices` series, set `is_limit_lock`, and make `compute_total_returns` use cumulative-return-across-the-lock-window (set `limit_lock_adj`) — with a golden/fixture test on a known locked sequence. Then staleness flags and the accounting_regime state machine.

## 2026-06-19 — M1 blocked on Matriks outage; baseline re-verified GREEN (milestone M1, in progress)
**Did:** Session START protocol only. Re-verified the known-good baseline (`make verify`) and attempted to unblock the M1 live-ingestion next action by re-running `make smoke` and probing each Matriks REST tool directly.
**Result:** **`make verify` GREEN — 102 passed / 5 skipped** (golden suite runs twice; standalone golden = 10 passed; invariants = 10 passed / 4 skipped). **`make smoke` is RED — Matriks is fully down:** *every* REST tool (`historicalData`, `marketPrice`, `fundamentalAnalysis`, `symbolSearch`) returns **HTTP 504 Gateway Time-out** after ~15s; `openapi.json` times out (HTTP 000). The nginx front responds at root (`/` → HTTP 200, fast), so this is a **Matriks-side upstream/application outage**, not auth/contract/our-network. Last session's 504 (then only `historicalData`) has widened to all tools. The adapter behaved exactly per §4 — `SourceUnreachable`, no fabrication, no placeholder.
**Decided:** Per CLAUDE.md §8 (unreachable source = hard stop), surfaced the outage to the user with options (build offline-buildable M1 slices / offline L2 roundtrip from committed goldens / halt-and-wait). **User chose HALT AND WAIT.** No new code written this session; repo left GREEN. No commit (nothing changed but this log entry).
**Open:**
- **Matriks REST is 504 across all tools — external outage, no ETA.** Nothing to fix on our side. M1's live fetch→parse→L2 roundtrip + `make smoke` cannot proceed until it recovers.
- All prior M1 open threads from the slice-1 entry below still stand (limit-lock censoring, staleness flags, accounting_regime tag/unskip `test_accounting_regime`, CPI-real-TRY via evds3, real delisted name end-to-end, full-dividend live reconciliation). Several of these (limit-lock, staleness, accounting_regime) are network-free and could be built offline when work resumes — user deferred them this session by choosing halt-and-wait.
**Next action:** Re-run `make smoke` (after `set -a && source .env && set +a`). If GREEN → wire the M1 end-to-end live ingestion (the slice-1 next action below). If still 504 → either wait, or build the network-free M1 slices (limit-lock / staleness / accounting_regime) offline with golden/fixture tests.

## 2026-06-19 — M1 slice 1: clean total-return core + EREGL/ASELS golden-master reconciliation (milestone M1, in progress)
**Did:** Built the financial-accuracy core of M1 as one vertical slice. (1) `tmkg/returns/total_return.py` — a **pure** (no-IO) USD-primary total-return constructor: corporate-action-adjusted close-to-close returns (Matriks W7 back-adjusted basis, *not* re-adjusted here — that would double-count) with dividends added as a **basis-invariant yield**, never as a back-adjusted cash amount (the basis trap). USD-primary = convert close→USD first then return; real-TRY cross-check via CPI deflation when CPI supplied, else left null (no EVDS wired yet — never fabricated). FX-missing steps yield `NaN`, not an interpolated rate (§4). Helper `dividend_yields_from_raw()` refuses an ex-date with no raw close. (2) `MatriksAdapter.parse_bars` / `parse_corporate_actions` — `historicalData`→`prices`-schema rows (handles both equity `allBars` and FX `bars` shapes), corp-actions parse that **drops+counts** blank/garbage ex-dates (the golden's ASELS key_fact), never guessing. (3) `tests/golden/test_total_return_reconciliation.py` — offline, deterministic reconciliation from the committed EREGL/ASELS OHLCV + USDTRY goldens. Removed a stray cloud-sync duplicate `tests/invariants/test_no_fabrication 2.py` (stale older copy of the tracked file, was being double-collected).
**Result:** **`make verify` GREEN — 103 passed / 6 skipped** (was 98/6; +5 golden tests, golden suite 5→10). Hand-verified anchors now frozen: **EREGL rights ex-date 2024-11-27 = TRY −1.3972% / USD −1.3803%; ASELS rights ex-date 2023-08-25 = TRY +2.1081%** — all small/economic, proving back-adjustment (a naive unadjusted series would gap ~tens of %). Dividend-yield mechanism + blank-ex-date refusal also tested.
**Decided:** Dividends enter total return as a **yield** (cash / raw close on ex-date), computed by the ingestion layer from RAW bars and consumed by the (adjusted-basis) constructor — sidesteps the back-adjusted-vs-nominal basis mismatch cleanly. No ADR (implementation choice, consistent with design §3/W7).
**Open:**
- **Live parse-roundtrip not yet confirmed.** Matriks REST is returning **HTTP 504 Gateway Time-out** right now (nginx) on `historicalData` — transient upstream outage. The adapter correctly **failed loud** (`SourceUnreachable`, no fabrication, §4). Offline reconciliation is fully green and the parsers match the exact golden shapes the smoke gate locks to live, so the path is sound — but a live fetch→parse→construct roundtrip (and `make smoke`) should be re-run once Matriks recovers.
- **This is slice 1 of M1, not the whole milestone.** Still owed before the M1 exit gate: end-to-end ingestion writing `prices`+`total_returns` to L2 bitemporally (+ FX into `factors`) and reading back via `PITAccess`; **limit-lock** ±10% censoring + cumulative-across-lock-window; **staleness** flags; **accounting_regime** tag on fundamentals (unskip `test_accounting_regime`); CPI-real-TRY via EVDS (evds3); ingest one **delisted** name end-to-end (real survivorship, not just the W2 mechanism). The full-dividend total-return reconciliation against a live fetch around EREGL's 2024-04-16 ex-date is still pending (needs raw bars on that ex-date — out of the captured golden windows).
- The dividend-yield path is unit-tested on a controlled fixture but not yet on real EREGL/ASELS dividends end-to-end (blocked on the same out-of-window raw-bar fetch + the 504).
**Next action:** Wire the **end-to-end M1 ingestion**: `MatriksAdapter` fetch→parse→`L2Store.write_parquet` for `prices` (EREGL/ASELS + USDTRY into `factors`), build `total_returns` through `PITAccess`, write the ingestion audit report, and add the L2 round-trip + PIT-read reconciliation test — once Matriks 504 clears (re-run `make smoke` first).

## 2026-06-19 — M0 executed end-to-end: data-access proof + L2 + PIT + id-bridge + survivorship (milestone M0 ✅)
**Did:** Ran M0_TASKS T1–T5 in order. **T1 (the [STOP] gate):** fetched `openapi.json` (22 camelCase tools), then proved the Matriks **REST** path live and wired `MatriksAdapter.fetch()`/`smoke_check()`; `make smoke` PASS. **T2:** implemented `L2Store` (DuckDB connect + `bootstrap_schema` from schema.sql + `write_parquet` PK-idempotent round-trip). **T3 (keystone):** implemented `PITAccess.series()` with a non-negotiable `knowledge_date <= as_of` filter and unskipped the **PIT-leak detector**. **T4:** built the `IdBridge` resolver (ticker↔ISIN↔kap_oid↔LEI) that refuses+logs ambiguity. **T5:** implemented `PITAccess.universe()` + the survivorship/W2 mechanism. Also strengthened the no-fabrication invariant (unreachable ⇒ `SourceUnreachable`, never a placeholder). Added ADR-0002; updated VERIFICATION/BUILD_PLAN.
**Result:** **`make verify` GREEN — 96 passed / 4 skipped** (was 80/5); `make smoke` PASS. New green tests: `test_matriks_live` (6), `l2/test_l2_roundtrip` (3), `test_pit_leak::test_no_read_returns_knowledge_date_after_as_of`, `test_idbridge` (4), `test_survivorship` (2), `test_no_fabrication` (3). Remaining 4 skips are correctly future-milestone: accounting_regime no-straddle (M1), no-network signal AST-scan (M3), provenance soft-edges (M1), fixtures-never-reach-L2 (M1).
**Decided (→ ADR-0002):** Ingestion transport = **REST** `POST /mcp-api/v1/tools/{camelCaseTool}/execute`. **Auth = single `X-API-Key: <username>:<key>` header — adding `X-Client-ID` makes the gateway 500 'Authentication failed'** (proven by elimination; the original stub's extra header was the bug). Response is an MCP envelope; payload = `json.loads(content[0].text)`. Smoke gate is two-mode: **value-match** the two immutable OHLCV anchors (real drift teeth), **reachability-only** for the curated/volatile single-call goldens, **skip** the multi-call composites — no fabricated value-match where data legitimately moves. Writes `data/cache/matriks_smoke_report.json`.
**Open:**
- **The reshape finding:** only the two OHLCV goldens are raw single-call captures that value-match live. `declaration_dates`, `universe`, `foreign_flow` were **reshaped/curated at capture** (golden symbolSearch `data` = live's inner `index` object; declaration golden is a derived extract) and news/foreign-flow are volatile → they prove reachability, not value. **M1 follow-up:** re-capture raw single-call goldens for these so they graduate to value anchors.
- **id-bridge leg = `kap_oid`, not `mkkMemberOid`.** The v1 graph stores `kap_oid`; MKK's `mkkMemberOid` (Matriks/MKK id space) is a separate leg to reconcile when Matriks ingestion lands (M1). All four anchors round-trip clean.
- **Survivorship is the mechanism, not a real name yet.** v1 graph has no time-varying listing (static `listing_status` only: EQUITY_TRADED / NON_EQUITY_ISSUER / EXTERNAL_PARENT, no delisted-with-dates). T5 proves the W2 wall via `universe_membership` (new bitemporal L2 table) with controlled test rows. **M1:** ingest a real delisted BIST name end-to-end with sourced dates.
- schema.sql fix: `window` is a DuckDB reserved keyword → quoted in `betas`/`residual_corr`.
- `PITAccess.graph()` still a stub — deferred to M1 when time-varying edges (MEMBER_OF/CONTROLS history) carry `knowledge_date`. id-bridge reads the graph directly (identity is not a time-varying signal read), same posture as L2Store on the quant side.
**Next action:** Begin **M1 — clean return series.** First slice: the Matriks OHLCV + corporate-actions + dividends ingestion adapter → L2 `prices`/`total_returns` (bitemporal), then USD-primary total-return construction with the EREGL/ASELS bedelsiz golden-master reconciliation (BUILD_PLAN M1 exit). While there, re-capture the raw single-call goldens to upgrade them to value anchors.

## 2026-06-19 — v2 build kickoff: plan + protocols laid down (pre-M0)
**Did:** Authored the v2 build scaffolding — `CLAUDE.md` (constitution: invariants, data-access contract, session protocol), `BUILD_PLAN.md` (milestones M0–M8 with go/no-go gates), `VERIFICATION.md` (invariant suite + golden masters + promotion-gate mechanics), this log, and `decisions/ADR-0001` (L1 store = Kuzu). Then pre-snapshotted **10 golden samples** from the Matriks MCP to `tests/golden/matriks/` (OHLCV around the EREGL/ASELS rights ex-dates, KCHOL declaration dates, adjusted-vs-unadjusted FY2024 fundamentals, USD/TRY + XU100 factor series, GARAN foreign flow, news feed, BIST30 universe) + a MANIFEST mapping each to its invariant/gate. Then **scaffolded the v2 repo**: `src/tmkg/pit/` (bitemporal access gateway — the keystone), `src/tmkg/l2/` + `schema.sql` (DuckDB quant store, all tables bitemporal incl. signal_registry), `src/tmkg/ingest/` (adapter contract + Matriks stub + audit-report writer), placeholder packages `returns/`/`factors/`/`signals/`; the verification harness (`Makefile`, `scripts/verify.sh`, `scripts/smoke_data_access.py`, `pytest.ini`, `tests/invariants/` §5 guards + `tests/golden/test_golden_integrity.py`); wired the Matriks connector (`.mcp.json` via `${MATRIKS_API_KEY}`, real key in gitignored `.env`); updated `requirements.txt`; wrote `M0_TASKS.md`. Stubs raise `NotImplementedError` with `TODO(M0)` — no pillar logic yet.
**Result:** Planning docs complete and cross-checked. Matriks MCP verified **reachable from Cowork**; all 10 golden JSON files valid; reconciliation answers pinned — EREGL −1.40% across the 2024-11-27 rights ex-date (no gap → back-adjusted), KCHOL FY2024 revenue adjusted/unadjusted ratio 1.31, declaration dates back to FY2008, AKDE/MKK foreign-flow license live. Verification suite: N/A (not yet wired — that is M0).
**Decided:** L1 store = KuzuDB (ADR-0001). Data access = same MCP connectors as Cowork, via checked-in `.mcp.json`; architecture is MCP → ingestion adapter → local Parquet/Kuzu cache; signal layer never hits the network; market data is never fabricated.
**Open:**
- **Reachability proven from Cowork, NOT yet from Claude Code.** The golden samples prove the *data* exists and the *Matriks* tools work here; they do not prove Claude Code's environment can reach it. Re-fetching to match the golden samples is M0 T1, the hard gate. **(Update: auth now fully documented from `Matriks_MCP_Dokumani.pdf` — REST at `mcp-api/v1/tools/{tool}/execute` with `X-API-Key: <username>:<key>` + `X-Client-ID`, and header-auth MCP at `/mcp`. Neither is the `/claude` OAuth endpoint, which is Claude-Desktop-only. `.mcp.json` corrected from the wrong `/claude` Bearer guess to `/mcp` header-auth. The ingestion adapter uses REST — Python cannot call MCP tools; the header/URL helpers are implemented + verified, only the httpx call remains.)**
- **Only Matriks is snapshotted.** EVDS, KAP-direct, GDELT, and the W3 proxies (FRED VIX, EEM, scraped CDS) have no golden sample yet — capture when wired.
- **Two Matriks param quirks documented in the MANIFEST:** `institutionalFlow` ignored `foreignPeriods` (returned latest month); `newsAndEvents` did not strictly honor `category`. Verify specific-period/historic foreign pulls and filter news client-side.
- v1 equities core (802 Companies, Kuzu) is the L1 starting point; confirm it still loads and `pytest` is green before extending.
- Cowork-sandbox Kuzu lock gotcha is a sandbox artifact and *should* not affect native Claude Code on macOS — verify early; if "Operation not permitted" on `.shadow`/`.wal` appears, build on a local path and copy the `.kuzu` file.
**Scaffold verification (this session, in-sandbox):** all new stubs compile (`py_compile`) and import (stdlib-only deps); `PITAccess(None)` raises `PITViolation` (the first real invariant test is green); `.mcp.json` is valid JSON; secret scan confirms `sk_live_` appears **only** in the gitignored `.env`. The full `pytest` suite (incl. the v1 ~70 + new golden/invariant tests) must be re-run natively by Claude Code — the sandbox lacks the venv/duckdb/kuzu.

**Next action:** Execute `M0_TASKS.md` **T1** — `set -a && source .env && set +a`, fetch `https://mcp.matriks.ai/openapi.json` to confirm the REST tool slugs/params, implement `MatriksAdapter.fetch/smoke_check` over the **REST API** (`X-API-Key: <username>:<key>` + `X-Client-ID`), and get `make smoke` to PASS (live response matches the golden samples). Record the working transport in `decisions/ADR-0002`. If unreachable after checking creds + openapi.json → STOP, do not fabricate.

---

## 2026-06-25 (later) — M7 prep: KAP `includeNewBusiness` tier-1 proof — **[STOP] Matriks auth down**

**Goal (M0 discipline):** prove M7's tier-1 supply-chain spine is real — pull structured business-contract filings (Matriks `newsAndEvents` + `includeNewBusiness`, which delivers KAP "Yeni İş İlişkisi" rows with counterparty/`desc`/`amount`/`cur`/`date`/`url` + materiality ratios `rt`/`marValRt`, per `data-sourcing-v2.md` W6), confirm those fields are present + parseable for a handful of BIST names, snapshot a golden. Outcome decides whether M7 starts on tier-1 or falls back to tier-2/3.

**Blocker — HARD STOP (CLAUDE.md §4/§8):** the Matriks ingestion path is unreachable. The REST gateway (`https://mcp.matriks.ai/mcp-api/v1`) is reachable and processing auth but **rejects our credentials credential-wide**: every tool — `newsAndEvents` (incl. `includeNewBusiness`), `news_and_events` bare, and `historicalData` (which has worked since M0) — returns `HTTP 500 {"error":"INTERNAL_ERROR","message":"Authentication failed"}`. Creds in `.env` are well-formed and unchanged (numeric `MATRIKS_USERNAME` len 5, `MATRIKS_API_KEY` = `sk_…` 40 chars, correct REST_URL); auth header is the ADR-0002 `X-API-Key: <username>:<key>` only. Since failure spans tools that worked earlier today and the gateway answers cleanly, this is a **server-side credential rejection** (key rotated/revoked/expired) or a Matriks auth outage — not a contract drift or a code bug. The interactive `matriks-finance` MCP (the alt golden-capture channel) never finished connecting this session either.

**Did NOT fabricate / mock / stub any business-contract data (§4 rule 2).** No golden written, no L2 write, no adapter change. Probe scripts live only in the session scratchpad.

**Next action (needs user):** refresh / re-issue the Matriks API key (or confirm whether this is a known transient Matriks-side auth outage), update `.env`, re-run the `historicalData` smoke to confirm creds are live, **then** resume the `includeNewBusiness` probe → golden (`tests/golden/matriks/newbusiness_*.json`) → invariant test → M7 tier-1 go/no-go. Repo left GREEN (no changes).

## 2026-06-26 — M7 unblocked + tier-1 NO-GO (feasibility) → tier-3 sector-IO NO-GO (OOS) — **[STOP] surfaced**

**Matriks back online.** The credential-wide REST auth rejection from 2026-06-25 was a transient Matriks-side auth outage, now cleared — `make smoke` PASS (Matriks + EVDS), baseline `make verify` GREEN (398 passed / 1 skipped). The interactive `mcp__claude_ai_Matriks_ai__*` MCP also connected this session (used only for an independent reachability cross-check; ingestion stayed on the REST adapter per §4). No `.env` change was needed — the same `sk_…` key now authenticates.

**M7 tier-1 (firm-level KAP new-business) — NO-GO on DATA FEASIBILITY (before M4).** Resumed the `includeNewBusiness` probe. Finding: the REST `includeNewBusiness` flag is now ignored by the gateway (it produced the clean `amount`/`cur`/`rt`/`marValRt` fields in the June *interactive-MCP* probe — that was the MCP's post-parsed convenience output, NOT the REST contract). Via the contract-legal REST path the "Yeni İş İlişkisi" disclosures arrive as KAP news items whose structured table lives in the `content` HTML (`newsHeadlineSearch="iş ilişkisi"`, `newsSource=["KAP"]`, `newsCount<=100` — 200 returns empty). Built `scripts/probe_m7_newbusiness.py` (parser + counterparty classifier + listed-BIST matcher) → `data/cache/m7_newbusiness_coverage_report.json`. **Over 290 disclosures (Jan-2025…Jun-2026): unnamed 33%, government 12%, foreign 14%, anonymized 13%, named-domestic 28%.** Of named-domestic, the token-matcher's UPPER bound of listed-independent hits is 13.1%, but manual inspection leaves **~1 genuine independent listed-BIST counterparty per 100 disclosures** (rest are generic-token false positives or intra-group = the already-priced null). Materiality ratios disclosed in only 13%; amounts are free-text only. Market-wide that's **~10-15 tradeable listed-to-listed firm-level edges/yr, almost all one-off** — structurally far too sparse for the cross-sectional differential-exposure sort tier-1 requires. Exactly the W6 caveat + the risk-register's "linked-firm effect unproven on BIST." **No L2 write, no golden, no fabrication (§4).**

**User go/no-go (§8, AskUserQuestion):** chose **"Pivot to tier-3 sector-IO"** (drop firm-level; test the OECD-ICIO sector lead-lag as the last tradeable linkage hypothesis through M4).

**M7 tier-3 (sector-IO lead-lag) — NO-GO on OOS PREDICTABILITY.** Ran the cheapest-to-kill test of the tier-3 *premise* BEFORE sourcing OECD-ICIO: does any stable sector-level lead-lag survive OOS in the M2 residual returns? Built `scripts/probe_m7_sector_io.py` → `data/cache/m7_sector_io_feasibility_report.json`. Sector residual series (21 sectors with ≥8 names/day, 604 trading days) from L2 `residuals` × graph `IN_SECTOR`; train/test split; both a GENERIC ridge-fit full lead-lag matrix and the SPECIFIC textbook supplier→customer IO pairs (energy→industry, base-metal→metal-goods, chemicals→textile, cement→construction…), at daily/weekly/biweekly/monthly horizons. **Result: dead at every horizon.** In-sample IC +0.24 (t=18) collapses to **OOS IC −0.005 (t=−0.49)** daily; +0.003 / +0.017 weekly/biweekly; the monthly horizon (where customer-momentum should peak) shows +0.037 generic IC but on only **13 independent OOS blocks** — sample-starved, point estimates zero-to-negative, textbook mean OOS corr −0.097. Top in-sample lead-lag pairs agree OOS at coin-flip (7/15). **Sourcing OECD-ICIO cannot rescue this**: it only weights sector-pair lead-lags that themselves carry no stable OOS signal, and the monthly horizon needs ~10+ yr to test honestly (we have 3). Same wall as M6's small-sample/overlap-artifact diagnosis.

**Net: all THREE pillars now NO-GO.** M3/M5 correlation (genuine edge too thin to clear costs), M6 event (structurally dead), M7 supply-chain — tier-1 firm-level (data too sparse), tier-2 intra-group (the already-priced null the gate exists to reject), tier-3 sector-IO (no OOS predictability, monthly sample-starved). This is a **project-level [STOP]**: the firm-linkage / 3-pillar alpha thesis on BIST residual returns appears exhausted as a *tradeable cross-sectional* edge. The honest substrates built remain valuable (PIT/bitemporal core, USD-TR returns, factor/residual machine, M4 promotion judge, M6 channel-stress risk-spine, tier-1 new-business as an exposure annotation).

**Repo state:** GREEN — `make verify` 398/1, `make smoke` PASS. Two reproducible probe scripts + two coverage reports added; **no L2 writes, no schema/adapter changes, no fabricated data, no ADR yet** (NO-GO ratification awaits user sign-off per §8).

**Next action (needs user):** decide how to record/proceed at the 3-pillar [STOP] — (a) ratify M7 NO-GO + write ADR-0006 and treat the alpha-search as concluded, repositioning the deliverable as the honest research substrate + risk spine; (b) demand a fuller OECD-ICIO-based tier-3 test (build the ICIO ingestion + BIST crosswalk) before accepting, in case the cheap proxy missed a sparse monthly effect; or (c) open a new direction (e.g. M8 hardening, or a non-cross-sectional use of the linkage graph). Recommendation: (a) — the OOS evidence across horizons is consistent and the monthly sample wall is structural, not fixable by more weighting.
